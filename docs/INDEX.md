# Index des documents — ce que chacun commande, et où en est le projet

*Écrit le 10/09/2026. Ce document ne décide rien : il **oriente**. La règle du
dépôt ne change pas — les feuilles de route et les cahiers des charges sont les
critères d'acceptation et l'ordre de marche, et c'est dans EUX que se tranchent
les choix, jamais ici. Quand cet index et un document se contredisent, c'est le
document qui a raison et cet index qui est périmé.*

**Une page consultable en donne le même contenu** :
[`ordre-de-marche.html`](ordre-de-marche.html), publiée à
<https://claude.ai/code/artifact/549ce2bb-a1e1-4a98-b0ac-3b4921bb42f1>. Elle est
un RENDU de ce fichier, pas une seconde source : deux copies du même contenu
divergent toujours, et c'est ce fichier-ci qui a raison. Qui modifie l'un
refait l'autre.

---

## 1. Comment lire le dépôt

Trois niveaux, et ils ne se confondent pas :

| Niveau | Ce que c'est | Qui commande |
|---|---|---|
| **Feuilles de route** (`ROADMAP-*.md`) | l'ordre de marche, phase par phase, avec les mesures qui les ont tranchées | elles décident **quoi faire ensuite** |
| **Cahiers des charges** (`CDC-*.md`) | ce qu'un chantier doit tenir, écrit **avant** de l'écrire | ils décident **quand c'est fini** |
| **Références** (`ARCHITECTURE.md`, `MODE-EMPLOI.md`, `GUIDE-*.md`) | l'état des lieux et les modes d'emploi | ils **décrivent**, ils ne prescrivent pas |

Deux axes de travail coexistent, et l'un passe devant l'autre quand il faut
choisir : **le DAW d'abord**, la chaîne d'analyse ensuite.

---

## 2. Index

### Feuilles de route

| Document | Ce qu'il commande | État |
|---|---|---|
| [`ROADMAP-daw.md`](ROADMAP-daw.md) (30 412 l.) | faire du démonstrateur un logiciel de studio « digne de Cubase, Live, FL Studio » | **364 titres de phase** écrits (D0 → D371), tous clos — relevé du 20/09 (333 le 14/09 au soir, 280 le matin même, 90 au relevé du 11/09) ; l'index en comptait 62 depuis D62 ; le document est en régime d'**audits successifs** (D11, D13-D16, D22-D25, D29-D40, D55, D60-D62) — et, le 14/09, **neuf phases trouvées en regardant l'application EN MARCHE** (D283-D291 : miniature des notes dans les clips, liste d'événements et lanes qui suivent la lecture, lanes alignées sur l'arrangement, navigateur trié et cherchable par nature) ; le soir, **D301-D303** : la lane de vélocité bornée et réglable (1366 × 768 : 58 → 110 px pour la grille), la garde des façades qui jugeait une passe transitoire, l'onglet Automation qui suit enfin la piste choisie ; puis **D304-D309**, trouvées en ouvrant les `.mid` de l'utilisateur : la console qui prend sa place, un format 0 découpé par canal, des clips et des machines General MIDI à l'ouverture — par la banque installée quand elle est là ; le 15/09, **D322** : le clip audio importé (ou enregistré) prend la couleur de sa piste, comme tout clip qui naît ; **D323** : plancher de 12 pt sur 28 polices écrites en dur dans les panneaux, gardé par `tools/police-plancher.sh` ; **D324** : la console écrit « Délai » et « Transp. » dans ses cases quand la place existe, jamais au prix de la valeur ; **D325** : les traits de note des clips à 2 px au moins et l'encre à 0,55-0,95, mesurés par `tools/encre-clips.py` ; **D326** : sous le piano roll, les lanes du bas suivent SA fenêtre (715 px d'écart → 0-1 px) ; **D327** : une automation en hertz se lit sur une échelle log (médiane des points 7 → 59 % de la lane) ; **D328** : la lane MIDI CC dessine par colonne au-delà d'un point par pixel (35 460 CC 7 : 20,3 → 1,9 ms) ; **D329** : le volume de canal MIDI (CC 7) joue enfin — au mixage, multiplié au fader (paliers 127/64/32 → 0 / −5,95 / −11,97 dB) ; **D330** : le panoramique MIDI (CC 10) s'ajoute au potentiomètre (gauche pleine : −52 dB à droite) ; **D331** : la plage de pitch bend d'un fichier (RPN 0) est lue par piste — un pli d'octave jouait un ton — et les trois tenues sont chassées à la mise en lecture ; **D332** : CC 74 (brillance) et CC 71 (résonance) pilotent la coupure et la résonance de la machine par le profil sémantique — la montée de filtre de « Children » joue (centroïde 507 → 114 Hz sur le palier sombre) ; **D333** : le clip d'une piste MIDI ouverte va de sa première note à sa dernière, à la mesure près — Children montre où chaque partie entre (16 clips aux bornes du fichier, au tick près) ; **D334** : un silence de huit mesures sépare deux clips — Children passe à 31 clips, le canal 12 n'a plus 160 mesures de silence sous un ruban ; **D335** : un contrôleur hors de tout clip est rattaché au début du clip suivant — lecture, chasse, export (Children : 5 839 → 5 947 contrôleurs exportés, une régression de D333 trouvée à l'export) ; **D336** : à l'écriture dans le piano roll le clip implicite reste ouvert (la deuxième note écrite tombait hors clip), et un verbe `VSM_NOTES` écrit des notes au banc ; **D337** : une note écrite hors des clips bornés d'une piste est couverte — le clip le plus proche s'étend jusqu'à elle (moins de huit mesures), sinon un clip naît sous elle (export 2/4 → 4/4 notes) ; **D338** : « Zoom : tout voir » serrait les rangs du piano roll à 9 px et les noms de touche à 6 pt — plancher de 15 px au cadrage automatique (ce qui ne tient pas se centre et défile), noms de touche à 12 pt à tout zoom, relevé `VSM_PIANOROLL_RANG` ; **D339** : la fenêtre « Reconstruction » restait à « Démarrage... » (JUCE lit le tube par `fread` de 4 096 octets), « Annuler » laissait demucs finir seul, et quitter pendant la séparation avortait l'application (code 134) — un processus de chaîne dans son propre groupe, lu par `poll`, arrêté en entier ; **D340** : quitter pendant une reconstruction demande d'abord (l'étape nommée, « Quitter et interrompre » / « Continuer »), puis la question du projet modifié ; le 18/09, **D341** : le bouton « Annuler » de la fenêtre de reconstruction était hors d'atteinte de tout banc — `cliquer:` dit désormais dans QUELLE fenêtre il a pris le bouton et COMBIEN en portent le nom (« AMBIGU »), `cliquer:fenetre:<titre>:<nom>` désigne la fenêtre, `VSM_GESTE_APRES=<ms>:<geste>` joue un geste plus tard (aucun verbe n'agissait ailleurs qu'au démarrage), et `tools/reconstruction-annuler.sh` garde D339-D341 sur une vraie séparation (5 ratés sur 9 au témoin, 0 après) ; **D342** : le fader de la console avait **10 px de course pour 66 dB** (6,60 dB au pixel) — rayon de pouce de JUCE, rangée de départs prise sans départ, rail invisible — et il en a **86** (0,77 dB au pixel), avec une glissière, sa portion remplie et une échelle en décibels dont le 0 est prioritaire ; plancher de console 222 → 232 px (38 px pris à l'arrangement, contre 74 pour le plancher refusé), garde `tools/fader-console.sh` ; **D343** : quatre notes fausses sur 1 638 écrasaient la miniature du clip « bass » (ambitus brut 29-77, 49 rangs) — la fenêtre est celle qui tient **98 % des notes** (29-48, 20 rangs, **2,45×** de hauteur gagnée), les écartées posées sur la rangée du bord et COMPTÉES ; relevé `VSM_CLIPS_MINI`, garde `tools/miniature-clips.sh` ; **D344** : le vumètre d'une tranche était invisible au repos (contraste **1,07** avec le fond, mesuré aux pixels que le relevé désigne) — contour à **1,46**, graduations 0 et −6 dBFS, **témoin d'écrêtage qui reste allumé** et s'efface d'un clic ; et il devient MESURABLE (`VSM_MIXEUR_NIVEAU`, `VSM_VUMETRES`, le clic de banc qui atteint un composant sans être un bouton), garde `tools/vumetre-console.sh` — dont le premier contrôle validait le défaut (5,66 avant comme après) ; **D345** : le « nommé, non fait » de D327 fermé — **223 paramètres du parc sont en secondes** (contre 96 en hertz) et s'automatisaient sur une échelle linéaire : trente points géométriques de 3 ms à 4 s tenaient à **3,3 %** de la hauteur de la lane, ils en occupent **55 %** ; la borne du bas écrivait « 0.0 » pour 0,001 s ; règle écrite (unité multiplicative, une décade, minimum non nul), relevé `VSM_AUTOMATION`, garde `tools/automation-echelle.sh` avec ses deux témoins dans le même projet ; **D346** : et la lane logarithmique **gradue ses décades** (0 → 3 traits sur 0,001..8 s, 3 → 5 libellés) — « (log) » passe sur la borne du haut, la seule toujours écrite depuis qu'une décade occupe le milieu ; **D347** : la SEULE surface claire du logiciel était l'en-tête du tableau d'événements — 2 095 × 22 px à 164 de luminance, le défaut de `juce::TableHeaderComponent`, resté toute la vie du logiciel parce qu'un fond qu'on oublie ne casse rien et ne fait échouer aucun test ; balayage `tools/surfaces-claires.py` et garde `tools/theme-sombre.sh` sur huit vues (2 ratés au témoin, 0 après) ; **D348** : la liste d'événements savait regarder et supprimer, **jamais modifier** — saisie en place sur position, numéro, valeur et durée, `setTrackEventField` dans `core/` (**6 tests**, 343 → **349**), une valeur hors bornes REFUSÉE et jamais bornée en silence, le pas annulable ; relevé `VSM_LISTE_EDITER`, garde `tools/liste-editer.sh` qui relit le **.mid exporté** (48/0/100/480 → 72/0/42/240) ; **D349** : le piège que D32.2 avait NOMMÉ (« un numéro en dur est un piège qui se referme au premier onglet ajouté ») était encore armé **quatre fois sur six** — les six onglets du dock se désignent désormais par leur nom, le relevé dit « demandé / obtenu », `VSM_VUE=?` énumère les verbes et la garde les REJOUE (`tools/onglets-du-dock.sh`, 13 ratés au témoin) ; **D350** : le navigateur n'était pas un verbe de vue — il l'est, il entre dans le rejeu (20 verbes, 0 inconnu) et sa fenêtre est balayée pour la première fois (0 surface claire) ; **D351** : un transport vu partir SANS ORDRE — *Play* allumé et la tête à 00:02,357 six secondes après l'ouverture, **4 fois sur 6** à 13:24 — puis **0 fois sur 9** une fois la trace posée : ni reproduit ni expliqué, donc **pas décrit comme un défaut**. `VSM_TRACE_TRANSPORT` nomme désormais l'origine de chaque ordre de lecture, `tools/transport-au-repos.sh` le verra s'il revient, et les gardes bornent leur fenêtre (une course en plein écran prend le clavier de l'utilisateur, et l'espace lance la lecture) ; **D352** : la liste **crée** enfin — `addTrackEvent` dans `core/` (4 tests, 349 → **353**), un bouton « + » dont la nature vient du filtre, et **le seul endroit du logiciel où se posent un changement de programme et une pression polyphonique** ; la garde a trouvé au passage qu'une note créée hors des clips bornés n'était **ni jouée ni exportée** (4 notes au .mid pour 5 créées) — la création passe désormais par le chemin de couverture de D337 ; **D353** : le **canal** d'un événement se modifie enfin — la décision est écrite (il ne choisit pas qui joue, il décide de ce qui SORT), saisie de 1 à 16 comme elle s'affiche et rangée de 0 à 15 dans le fichier, hors bornes REFUSÉ ; 353 → **355** tests core ; **D354** : « Quantifier » est gardé par le menu Édition depuis D240, mais pas par ses TROIS AUTRES portes (bouton, clic droit, raccourci) — et les trois mentaient : le geste est juste (huit notes hors grille, 17 · 217 · 491 … → 0 · 240 · 480 …), ce sont CINQ pannes muettes du banc qui le faisaient passer pour cassé — quatre sorties anticipées sans un mot, un bouton grisé écrit « cliqué », un geste différé joué après un export qui s'écrit au démarrage, une entrée de menu grisée dite « absente », et TOUTE la table de raccourcis du piano roll hors d'atteinte de tout banc depuis qu'elle existe (`VSM_TOUCHE=pianoroll:…`) ; garde `tools/quantifier.sh`, vue rouge sur trois témoins ; **D355** : le même geste porte jusqu'à TROIS noms selon la porte par laquelle on l'atteint — « Joindre » dans la fenêtre des raccourcis et « Fusionner » dans le menu, « Ne rien sélectionner » et « Tout désélectionner », « Ajuster à la fenêtre », « Zoom : tout voir » et « Afficher toute la piste » pour un seul Ctrl+0 ; la décision est écrite (le nom du MENU gagne, et un raccourci qui sert deux vues les nomme toutes les deux), 4 écarts sur 17 gestes à plusieurs portes → **0**, dans les DEUX langues ; garde statique `tools/noms-des-gestes.py` qui apparie par l'APPEL et non par le nom, et qui REFUSE plutôt que de rendre zéro quand elle ne reconnaît plus le code ; au passage l'inventaire A9 revient de ECRAN 15 à **10**, D354 l'ayant pollué avec cinq messages de banc ; **D356** : les trois portes font-elles la même CHOSE ? Mesuré sur huit gestes déterministes (19 courses, .mid exporté relu par multiensembles) : **0 désaccord** — un résultat négatif qui valait d'être écrit, trois chemins d'appel distincts pouvant diverger sans que rien ne le dise. Il a fallu rendre la mesure possible : l'export devient un GESTE (`VSM_GESTE_APRES=<ms>:exporter-midi:<fichier>`), sans quoi la porte du bouton — qui exige une sélection posée avant elle — n'était mesurable par aucun fichier (la panne que D354 n'avait pu qu'avertir), et `VSM_POSITION` remonte avant `VSM_TOUCHE` (verbe de scène, pas d'action : il servait le menu et pas le clavier). Garde `tools/portes-des-gestes.py`, vue rouge sur un bouton recâblé à 50 % — que la garde statique de D355, elle, ne voit pas ; **D357** : les gestes de SÉLECTION étaient les seuls du piano roll qu'aucun banc ne pouvait mesurer — `VSM_SELECTION` n'était écrit que par le dispatcher du MENU (mesuré : 1 ligne par le menu, **0 par la touche**), si bien que « Tout sélectionner » au clavier, le geste le plus courant d'un éditeur, ne laissait aucune trace ; le compte se dit désormais là où la sélection CHANGE (`notifyEditState`, et seulement aux changements), ce qui couvre aussi la souris. Et « Humaniser », que la garde de D356 écartait comme aléatoire sur une phrase écrite sans mesure, a une graine FIXE : il y entre, et sa reproductibilité est mesurée d'un processus à l'autre. Douze gestes couverts, 0 désaccord ; **D358** : la règle de D155 — « la touche d'une entrée de menu est DESSINÉE par JUCE, jamais écrite dans le libellé » — n'était jamais sortie de la barre de menus, et **dix-neuf libellés** nommaient encore leur touche en dur. Or la table des raccourcis est modifiable et persistée : mesuré, Ctrl+E rebindé en Ctrl+Maj+E donnait « Ctrl+Maj+E » dans la fenêtre des raccourcis et « (Ctrl+E) » dans le menu du clip — un libellé qui nomme une touche qui ne fait plus rien. 19 → **0**, garde `tools/raccourcis-affiches.py`. Trois défauts trouvés par les gardes et aucun par la relecture : une copie survivante de la même infobulle, onze infobulles qui perdaient leur touche (posées avant l'arrivée de la table) et **14 chaînes privées de traduction anglaise** par le raccourcissement de leur libellé ; **D359** : l'arrangement n'avait **jamais** consulté la table des raccourcis — sept touches écrites en clair, et un commentaire de D16.3 qui nommait le chantier depuis des mois. Un réglage de touche n'atteignait donc pas cette vue, et elle était de surcroît INMESURABLE : `createFromDescription` rend un caractère NUL, si bien qu'un gestionnaire qui reconnaît ses touches par `getTextCharacter()` est invisible à tout banc (« AUCUNE commande de ce clavier »). Dix commandes passent par la table ; mesuré avec le piano roll pour témoin, Ctrl+Maj+D duplique (2 clips, 16 notes) et Ctrl+D ne fait plus rien. La garde des portes atteint enfin l'arrangement (**14 gestes**) et y a trouvé deux défauts, dont un DANS LE BANC — le verbe de menu de clip vidait la sélection là où la souris la garde, ce qui rendait « Joindre les clips choisis » inatteignable ; **D360** : l'arbitrage laissé ouvert par D359 est tranché et écrit — l'aimantation se basculait par **S** dans l'arrangement et par **G** dans le piano roll, et c'est la touche de la TABLE qui gagne, parce qu'elle est la seule que l'utilisateur puisse changer. La grille de l'arrangement entre dans la table plutôt que de reculer sur une autre lettre en dur (`view.arrangementBarGrid`, **M** comme mesure). Il a fallu écrire un relevé pour mesurer quoi que ce soit : l'état est PEINT sur la règle, donc invisible à tout relevé de textes ; **D361** : la garde des noms atteint enfin l'arrangement — impossible avant que son menu porte la touche vivante (D358) et que son clavier consulte la table (D359). La règle y est différente et elle est écrite : entre deux vues, les noms n'ont pas à être les mêmes (« Fusionner » des notes, « Joindre les clips choisis »), mais l'entrée de la TABLE doit contenir le nom de CHAQUE vue, dans les deux langues. Un écart trouvé et corrigé (la table disait « joindre des clips » là où le menu dit « Joindre les clips choisis ») ; deux défauts de la garde attrapés avant d'accuser le logiciel, dont un décodeur d'échappements qui échouait en silence sur `\u00e0` ; **D362** : trouvée en regardant l'application ouvrir un VRAI morceau — `children-c3-plafond` (227 mesures) s'ouvrait sur **3,8 mesures, soit 1,7 %**, et il fallait presser Ctrl+0 à chaque ouverture. La vue se cadre désormais sur le morceau (103,7 %), la décision étant écrite : le format ne porte aucun état de vue, et le jour où il en portera un c'est lui qui primera. La garde a trouvé au passage un défaut plus ancien — « tout voir » cadrait sur la dernière NOTE (`lastUsedTick`) quand ce qui est dessiné est le clip, si bien qu'un projet uniquement AUDIO se cadrait sur un tick : c'est `lastSoundingTick` de D8.3 qu'il fallait, comme son en-tête le disait déjà. Une première hypothèse — les titres de tranche rétréciraient sous le plancher — a été mesurée au pixel et **réfutée** ; **D363** : un projet se rouvre là où on l'a laissé — `project.json` porte un bloc `view` FACULTATIF (zoom, défilement), écrit seulement s'il dit quelque chose, et c'est lui qui prime à l'ouverture comme D362 l'avait annoncé ; le cadrage reste le repli. Zoomé à 66,7 %, enregistré, rouvert à **66,7 %** ; 3 tests d'interchange (300 → **303**), dont un zoom absurde IGNORÉ plutôt qu'obéi. Une fausse piste détrompée par la seule mesure : remplir `LoadedBundle::document` ne servait à rien, `saveProjectBundle` REBÂTIT le document depuis le projet  ; le 19/09, **D364-D365** : A6 close en la mesurant à la taille réelle de l'écran (3 200 × 2 000, quatre vues, 0 surface claire), et le « reste nommé » de D364 clos par examen — une mesure RELATIVE se moque de la taille, seule une mesure ABSOLUE doit être jouée deux fois ; **D366** : **B5**, le corpus du banc synthétique qui ne portait ni longueur, ni notes brèves, ni voix — le lot `s2` (10 morceaux de 186 à 269 s, 13 à 16 sections, **22 891 notes mélodiques sous 120 ms** contre 0, 7 morceaux échantillonnés, 5 chantés), garde `tools/corpus-exigences.py` (0/4 sur l'ancien corpus, 4/4 sur le neuf), défaut du générateur inchangé **au bit près**, et deux défauts du moteur trouvés en écrivant — dont un garde-fou de rendu qui bornait la durée au lieu des échantillons, laissant passer onze gigaoctets en lot et interdisant un morceau de quatre minutes ; le 20/09, **D368** : une reprise après panne retrouvait le travail mais pas l'ÉCRAN — la sauvegarde automatique n'emportait pas la vue (le « reste nommé » de D363), et la session reprise s'ouvrait à 104,2 % là où on l'avait laissée à **66,7 %** ; mesurée sur une VRAIE panne (l'application tuée, puis « Récupérer »), garde `tools/autosauvegarde-vue.sh` à six contrôles vue rouge sur son témoin. Deux défauts du BANC trouvés parce qu'il accusait à 1135,4 % — `VSM_RECUPERER` répondait depuis le constructeur quand le clic répond d'un rappel modal, et le relevé partait avant la reprise —, et un relevé qui écrivait **« soit 100,0 % »** pour un morceau VIDE, dénominateur nul compté comme un cadrage parfait ; **D369** : le morceau se rouvrait avec son zoom mais sans sa PISTE ni son ÉDITEUR — cinq champs de vue de plus (piste choisie, zoom, défilement, note du haut et hauteur de rang du piano roll), la règle de partage de D363 appliquée champ par champ (ce qui dépend du morceau va dans le projet, l'onglet du dock reste aux préférences), et **la cause de D368 retirée** : la vue se compose désormais en UN endroit (`vueActuelle`) là où les deux chemins d'écriture la bâtissaient chacun de leur côté. Réouverture : piste 0 / haut 84 au témoin, **piste 2 / haut 61** avec la reprise ; garde `tools/vue-du-morceau.sh` (6 contrôles, 2 ratés au témoin), relevés neufs `VSM_PISTE_CHOISIE` et la fenêtre de hauteurs dans `VSM_PIANOROLL_ZONES` ; **D370** : le « reste nommé » de D361 — les deux RÈGLES portent le même menu de repères, écrit deux fois (D83, D218), et les numéros 2 et 3 y sont **échangés**. Mesuré avant d'accuser : chaque gestionnaire est cohérent avec SON menu, les deux se comportent juste, ce n'est pas un défaut — c'est le piège de D349, et il se refermerait au premier qui recopie un gestionnaire d'une règle vers l'autre (posé pour voir : « Renommer ce repère… » appelle alors `onMarkerRemoved`, le libellé qui renomme EFFACE). Garde statique `tools/menus-des-regles.py` — appariement par le LIBELLÉ, comparaison sur l'ACTION, jamais sur le numéro —, 3 contrôles, et l'affirmation de D361 (« aucun raccourci en commun avec la table ») devient une mesure ; **D371** : la garde de D358 ne lisait que le PREMIER littéral d'un `tr(…)` — or le C++ colle les littéraux adjacents et ce dépôt coupe ses phrases sur plusieurs lignes, si bien que « Enregistrez d'abord le projet (Ctrl+S) » était invisible, la touche vivant dans le second morceau. Elle lit désormais TOUT l'appel : **5 libellés** nommant `Ctrl+S` en dur dans les boîtes « Projet jamais enregistré », corrigés par `toucheEntreParentheses` (qui n'écrit RIEN quand la commande n'est liée à aucune touche), 4 paires de traduction anglaise suivies ; 15 ternaires récupérés, et **36 libellés assemblés sur une variable DITS à chaque course** plutôt que tus — un zéro sorti d'une mesure qui ne regarde pas n'est pas un zéro |
| [`ROADMAP-fusion.md`](ROADMAP-fusion.md) (4 013 l.) | la chaîne d'analyse : d'un enregistrement à un projet jouable | phases 8 à 11 **toutes closes** ; le travail vit désormais dans les § 5 *bis* → *quindecies* (le 15/09, **§ 5 quindecies** : la reconstruction s'ouvrait à 120 BPM — le tempo est estimé sur le mélange, 10/10 morceaux du banc à ±2 BPM, `tools/tempo-estime.py` ; « B4 Wuz Then » : 126 BPM) (les pannes muettes, les hypothèses H13 à H25) ; **D282 tranchée le 14/09 au soir** : la coupure adaptée de la basse ne traverse pas la chaîne (1 critère sur 4), refusée comme défaut, gardée en option |
| [`ROADMAP-interop.md`](ROADMAP-interop.md) (276 l.) | Python ↔ CLAP ↔ formats d'échange | **P0 à P10 tous faits** — c'est la seule feuille de route entièrement terminée |
| [`ROADMAP-apprentissage.md`](ROADMAP-apprentissage.md) (1 475 l.) | les modèles appris (classifieur de machine, gabarits de batterie, estimateur) | A0, A1, A2, A4 faites ; **A3 refusée par la mesure** ; **A5 close sauf l'écoute** ; **A6 CLOSE le 12/09** (§ A6.1 → A6.8, relu le 20/09 — cette ligne l'a dite « en cours » huit jours de trop) : le corpus est réengendré sur **58** machines mélodiques et non 59 (`vsm.fmdrums` est une boîte à rythmes), 273 061 exemples ; le premier modèle est REFUSÉ par la mesure (17,8 % de top 1), H28 réfutée, **H29 tenue** — changer de famille d'estimateur donne **83,6 %** et fait tomber le critère A1.1 sur le parc entier ; le modèle à 58 machines est ADOPTÉ et **A5.2 atteinte pour le modèle** (§ A6.8 : 0 désignation confiante sur un piano acoustique, contre `vsm.sh101` à 0,99 au témoin). Restent nommés `vsm.sampler` (corpus de frappes) et A5.2 côté chaîne, la présélection apprise |

### Cahiers des charges

| Document | Le chantier | État |
|---|---|---|
| [`CDC-machines-manquantes.md`](CDC-machines-manquantes.md) (2 681 l.) | élargir le parc de machines, et le prix de chacune | § 1-8 livrés ; **21 hypothèses H tranchées**, 20 succès, 1 échec assumé (la guitare électrique, non écrite) ; § 33 donne le **coût CPU mesuré des 64 machines** |
| [`CDC-detection-multipiste.md`](CDC-detection-multipiste.md) (2 017 l.) | la parité des pistes : un original à N parties doit donner N pistes | § 4 ordre de marche : **les 9 cases COCHÉES**, aucune ouverte (relu le 20/09) — le § 4.3, que cette ligne disait « se croit ouvert », l'est depuis le 03/09 : verdict du mélange, réverbération cherchée, original chargé pour l'écoute A/B. Ce qui reste est écrit à la fin du document et n'est pas une case : l'effet de bout en bout du témoin de coupure sur « B4 Wuz Then » n'est pas remesuré (il demande une course complète) |
| [`CDC-separation-par-synthese.md`](CDC-separation-par-synthese.md) (981 l.) | la boucle résiduelle : soustraire ce qu'on sait rendre, reséparer le reste | câblée et testée de bout en bout ; § 5 porte les décisions tranchées en écrivant, § 6-7 la campagne R1 |
| [`CDC-banc-synthetique.md`](CDC-banc-synthetique.md) (989 l.) | mesurer la chaîne sur des morceaux à vérité connue | **campagne S1 mesurée** (20 morceaux) — et elle a réfuté la chaîne à tous les étages ; § 7 liste ce que le banc ne sait pas encore faire |
| [`CDC-multisample.md`](CDC-multisample.md) (610 l.) | `vsm.multisample` : l'acoustique par échantillons | M1 (profil piano) et M2 (import SoundFont) livrés |
| [`CDC-etirement-temporel.md`](CDC-etirement-temporel.md) (516 l.) | qu'un clip audio suive le tempo | D12 livrée (WSOLA + vocodeur de phase) ; § 9 porte D54, la transposition |
| [`CDC-apprentissage.md`](CDC-apprentissage.md) (369 l.) | ce que les modèles appris doivent tenir | rempli par `ROADMAP-apprentissage.md` |
| [`CDC-nouvelle-machine.md`](CDC-nouvelle-machine.md) (259 l.) | la liste de contrôle d'une machine neuve | **permanent** — ses cases se cochent à chaque machine, jamais dans le document |
| [`CDC-import-daw.md`](CDC-import-daw.md) (178 l.) | importer un projet FL Studio / Ableton / Cubase | livré (D7) ; Cubase reste hors de portée, et le document dit pourquoi |

### Références

| Document | Ce que c'est |
|---|---|
| [`../ARCHITECTURE.md`](../ARCHITECTURE.md) (4 722 l.) | la conception complète, machine par machine, avec les mesures |
| [`../README.md`](../README.md) (739 l.) | la porte d'entrée : quoi, comment compiler, comment s'en servir |
| [`MODE-EMPLOI.md`](MODE-EMPLOI.md) (1 402 l.) | le manuel de l'application |
| [`GUIDE-ajout-machine.md`](GUIDE-ajout-machine.md) (508 l.) | comment on écrit une machine, pas à pas |
| [`../analyse/PONT-VSM.md`](../analyse/PONT-VSM.md) (102 l.) | le protocole du pont Python ↔ moteur |

---

## 3. Où en est le projet, en chiffres

| | |
|---|---|
| **Machines** | **63**, plus la tonalité d'essai — 64 identifiants au registre ; **16 effets** d'insert ; **63 façades** dessinées |
| **Tests** | **2 001 côté moteur**, relevé le 20/09 — 1 303 audio, **355 core**, **307 interchange** (304 le 19/09 ; **+3 par D369** : l'aller-retour des cinq champs de vue neufs, une vue qui ne porte que le piano roll et s'écrit quand même, des valeurs absurdes ignorées), 25 clap, 11 panels — et **223 côté Python** (214 le 18/09 ; +9 par D366-D367 : les trois exigences du corpus B5, la hauteur sonnante et ses butées, le patch mal accordé retiré) |
| **Lignes** | `analyse/` 132 k · `audio/` 69 k · `app/` **33 k** · `interchange/` 20 k · `core/` 16 k |
| **Vérification** | `ruff check .` (lint) et `mypy analyse tools` (types, 131 fichiers) |

**Le déséquilibre qui ouvrait `ROADMAP-daw.md` s'est refermé.** Le § 1.1 mesurait
`app/` à **7 303 lignes** contre 95 837 pour `analyse/` — « le parc a été nourri
sans relâche ; l'atelier est resté un démonstrateur ». `app/` en fait
aujourd'hui **32 873**, soit **4,5 fois plus**, et les cinq critères du § 2
(sauvegarder, faire entrer du son, arranger, mixer, exporter) sont tenus —
posés par D0 à D10, puis **vérifiés sur un projet réel** par D45 (ouvrir et
réenregistrer une reconstruction ne perd rien) et D46 (l'export du DAW et
`vsm-render` rendent le même son).

**Ce que la mesure a refusé, et qu'il ne faut pas rouvrir sans raison neuve :**

- **l'estimateur de paramètres appris** (A3) — le fossé de domaine ; code
  conservé et documenté ;
- **la guitare électrique modélisée** (H10) — mesurée, écart insuffisant, la
  machine ne sera pas écrite ;
- **la reconstruction de la voix** — reportée telle quelle, et c'est le plus
  honnête ;
- **l'étirement temporel comme besoin de mesure** (D11.8) — c'est un besoin de
  production, tranché et livré séparément en D12.

---

## 4. Liste de travaux

Ordonnée : ce qui ment d'abord, ce qui manque ensuite, la documentation en
dernier. Chaque élément porte sa source ; **aucun n'est inventé ici**.

### A. Ce qui est nommé, mesuré, et pas encore corrigé

> **ÉTAT AU 19/09/2026 : les quarante-deux anomalies sont CLOSES** — A6, la dernière, l'a été par D364 en la mesurant à la taille réelle de l'écran ; **A42** (quinze façades sous 18 px à la largeur par défaut), ouverte par le balayage de D294 à 13:00, est close par D300 à 16:00, la garde rendant 0 / 63.
> **Quinze ont été ouvertes ET fermées le même jour** — A27 à A41 —, dont **six qui
> détruisaient ou perdaient des données sans un mot** : la piste gelée muette à
> l'export (A32), le profil de multi-échantillons effacé par un Ctrl+S (A34), le
> plugin tiers qu'un projet ne retrouvait jamais (A36), les cinq entrées du menu
> Fichier qui jetaient un projet modifié sans demander (A37), la fenêtre
> d'historique qui s'ouvrait vide (A40) et **le premier point d'automation qui
> effaçait toutes les courbes du projet** (A41, 606 points → 1).
>
> **ET LA DERNIÈRE EST TOMBÉE LE 19/09 : la section A est CLOSE.** A6 — « la
> barre de transport prend deux rangées faute de 1 400 px de large » — a été
> mesurée à la taille RÉELLE de l'écran (3 200 × 2 000, et non les 1 280 px des
> bancs) : elle tient sur **une rangée et 56 px** dès 2 240 px. Le compromis
> n'existait que dans la fenêtre des gardes. **D364**, et aucune ligne de code
> n'a changé.

| # | Travail | Source | Chiffre en main |
|---|---|---|---|
| ~~A1~~ | ~~Deux cellules de façade n'ont aucune hauteur~~ — **CLOS par D63** : la façade réclame la hauteur de sa grille et défile en dessous | `ROADMAP-daw.md` D63 | 2 boutons à 0 px → **0** |
| ~~A2~~ | ~~Quatre boutons du Minimoog sous 18 px~~ — **CLOS par D64** : son mélangeur est une bande verticale sur la machine d'origine, le dessiner en carré coûtait 11 px de large | `ROADMAP-daw.md` D64 | 23 → 4 → **0 / 49** sous 18 px |
| ~~A5~~ | ~~Quatre façades sur 63 gardent des boutons sous 18 px~~ — **CLOS par D70**, et le chiffre de D67 était faux : le sampler comptait **64** boutons sous le plancher et non 4, soit **98** pour le parc et non 37. Aucune voix n'a été empilée : c'est la grille INTERNE du bloc qui change, une colonne ne portant jamais deux pièces | `ROADMAP-daw.md` D64 → D70 | 98 boutons sous 18 px → **0** ; 4 façades → **0 / 63** |
| ~~A3~~ | ~~La molette de hauteur manque à six machines~~ — **CLOS par D26 (12/09)** : les six plient, chacune mesurée SUR LE RENDU par le critère écrit (« la note du dessus à 1 % près »), banc neuf `test_molette_hauteur.cpp` avec témoin et contrôle. Deux ont réfuté ce que la phase supposait : l'e-piano joue juste et c'est l'autocorrélation qui se trompait de −52 cents sur un timbre inharmonique ; la guimbarde rend 82 Hz à toutes les notes (sa lame est fixe), donc sa molette déplace le FORMANT et son critère est exact — plier de douze demi-tons sonne comme la note douze plus haut. Cinq tests qui gardaient le refus sont retournés, avec la raison écrite. *Constat d'origine* où un musicien plie la note (e-piano, clavinet, guimbarde, vielle, mandoline, kalimba) — un travail par machine, sur son modèle physique. *La flûte, septième de la liste d'origine, n'est pas dans le parc : hors build, résultat négatif assumé* | `ROADMAP-daw.md` D26 | 42 machines la tiennent, **6** la devraient |
| ~~A4~~ | ~~`--batterie-par-piece` n'a pas de mesure de distance~~ — **LIGNE PÉRIMÉE, close le 12/09** : l'effet EST chiffré, trois fois, et le tableau du § 4.4 le publie — v1 **+35,1 %** (pièces non calées), v2 **+28,2 %** (pas d'alternative d'usine), v3 **0,191036, le témoin AU MILLIONIÈME (+0,00 %)** avec 5 pistes (`usandthem-batterie-v3`, mesurée le 03/09 à 18:09). Le surcoût des deux premières versions était ENTIÈREMENT fait du calage et de l'alternative absente, jamais du découpage. *Troisième ligne périmée trouvée le 12/09* | `CDC-detection-multipiste.md` § 4.4 et § 8 | v1 +35,1 % → v2 +28,2 % → **v3 +0,00 %** |

| ~~A7~~ | ~~L'écran de rapport d'ouverture ne se photographie qu'une fois sur sept~~ — **CLOS par D72** : il passe de la boîte modale au volet de rapport, qui est dans l'autoportrait. Il y gagne le défilement, le bouton Copier et la réouverture par le menu | `ROADMAP-daw.md` D71 → D72 | 1/7 → **10/10** captures, pixel du volet lu sur l'image |
| ~~A8~~ | ~~Les deux lecteurs d'un projet ne numérotent pas les pistes pareil~~ — **CLOS par D75** : les deux fabriquent leurs phrases par les mêmes fonctions de `interchange/`, comptent depuis 1 comme la liste de pistes, et ne disent plus « silencieuse » d'un dossier ni d'une piste désactivée. L'application comptait déjà des deux façons à elle seule, en recopiant les phrases du chargement | `ROADMAP-daw.md` D71 → D75 | 9 et 4 lignes, 8 numéros faux → **5 et 5, diff vide** |
| ~~A10~~ | ~~Enregistrer détruirait ce qu'on n'a pas pu ouvrir~~ — **CLOS par D76, et c'était pire que le soupçon** : huit cas mesurés, dont trois ordinaires — ajouter ou supprimer une piste recréait TOUTES les machines, réglages remis à l'usine (et l'annulation passe par le même chemin : lu, pas mesuré, voir A11) ; le premier Ctrl+S effaçait les échantillons d'un sampler ; l'export du DAW rendait muette la voix de `sky-v4`, ce qui contredit D46. Les machines dont la piste n'a pas bougé ne sont plus recréées, un réglage sans machine vivante est gardé, et la capture prend les échantillons | `ROADMAP-daw.md` D75 → D76 | 4/9 réglages perdus → **0/9** dans 6 cas ; voix exportée silencieuse → **−6,44 dBFS** |
| ~~A11~~ | ~~Ctrl+Z n'agit que si le piano roll a le focus~~ — **CLOS par D79** : Ctrl+Z et Ctrl+Maj+Z passent par le même `pianoRoll_.undo()`/`redo()` que le menu, d'où que vienne la touche ; et le cinquième chemin de perte de D76 est joué — annuler, avant D76, perdait 4 réglages sur 9. `MainComponent::keyPressed` ne connaissait pas `EditUndo`. *Rectifié par D79 : cette ligne disait aussi que le menu Édition n'a ni « Annuler » ni « Rétablir » ; c'est faux, il les porte — c'est le menu contextuel du piano roll, et la recherche ne portait que sur `MainComponent.cpp`.* Trouvé parce que le banc de D76 n'a pas su annuler : son cas « annulation » a enregistré 3 pistes, c'est-à-dire un second ajout | `ROADMAP-daw.md` D76 → D79 | Ctrl+Z hors du piano roll : `VSM_TOUCHE : touche inconnue ou sans commande` |
| ~~A9~~ | ~~L'interface n'est traduite qu'en partie~~ — **CLOS par D130** : toutes les surfaces nommées ci-dessous lues en anglais — menus, panneaux et infobulles, fenêtres flottantes, façades, rapports ; les boîtes par échantillon (D91, D95-D114, D125), l'inventaire du code couvrant le reste (7 chaînes à la règle stricte, aucune affichée). **Le compte est GARDÉ depuis D150** : l'outil comptait cinq noms de composant pointés (`pistes.volume`, `pistes.pan`, `pistes.muet`, `pistes.solo`, `pistes.armement`) comme du texte d'écran — la dérive de 7 à 12 n'était pas une régression de traduction mais de comptage —, et il ignorait entièrement les libellés de pas d'annulation, dont deux n'avaient aucune clé (« Armement », « Master »). Les deux règles sont écrites dans son en-tête, et ECRAN est revenu à 7, SANS_PAIRE à 0, sous la règle stricte comme sous la règle large. Hors interface, dit par D130 : les noms de pistes que la chaîne écrit en français (« Batterie », « Voix ») restent des données françaises. *Historique* — la barre de menus l'est entièrement (227/227), mais les panneaux du dock, les fenêtres flottantes et les phrases fabriquées dans `interchange/` restent en français. Une chaîne sans traduction ressort en français, jamais vide | `ROADMAP-daw.md` D73 → D74 → D77 → D78 → D80 → D81 → D82 → D83 → D84 → D85 → D86 → D87 → D88 → D89 → D90 → D91 → D92 → D93 → D94 → D95 → D96 → D97 → D98 → D99 → D100 → D102 → D103 → D104 → D105 → D107 → D108 → D109 → D110 → D111 → D112 → D113 → D114 → D115 → D116 → D117 → D118 → D119 → D120 → D121 → D122 → D123 → D124 → D125 → D126 → D128 → D129 → D130 | **Traduit, et vérifié sur ce qui s'affiche** : la barre de menus (35 entrées identiques FR / EN, toutes neutres — D80, D81 ; le « 227 / 227 » de D73 était faux), les noms des gestes (D82), les menus de l'arrangement (D83), le volet du rapport d'ouverture et les phrases du moteur qu'il montre (D84, D89), les Préférences (D85), l'assemblage des prises et l'ordre de jeu (D86), les raccourcis, les associations MIDI et le navigateur (D87, D88), **l'export audio et l'export par stems**, fenêtres d'options et comptes rendus (D90 : lignes identiques FR / EN 29 → 10 sur deux projets, toutes chemins ou lignes vides ; fichiers rendus identiques octet pour octet). Le bouton d'annulation des boîtes dit « Cancel » : sept disaient « Undo », le sens du menu Édition (D90, `trSelon`). **Les boîtes de dialogue** — aller à la mesure, programme MIDI, presets de piste et d'effet, repères, clips, renommage en série, report en audio, dépôt d'un fichier — sont traduites et photographiées dans les deux langues (D91 : français à 0 pixel du témoin pour 10 sur 11, l'onzième ne différant que par un chemin ; `VSM_MENU_CONTEXTE` et `VSM_DEPOSER` ouvrent ce que la barre de menus n'atteint pas). Table à **1 366** paires, **110** modèles de phrases (D115). La bascule en direct rend la même image que le démarrage (D78). **Le rapport de reconstruction** est traduit à l'affichage, phrases de la chaîne comprises (D92 : lignes identiques FR / EN 97 → 30 sur quatre rapports, toutes dossier, lignes vides ou « stem → machine · distance » ; les nombres identiques ligne pour ligne). **Le rapport d'import d'un autre DAW**, ses échecs compris (le `.cpr`, les messages du lecteur et de la décompression), est traduit à l'affichage, et **les deux rapports suivent la bascule en direct** : chaque client du volet refait le sien (D93 : lignes identiques FR / EN 43 → 13 sur les trois projets d'épreuve et un `.cpr`, toutes noms de format ou lignes vides ; rapport de reconstruction basculé identique à D92 lancé en anglais, au caractère près). **Les panneaux toujours visibles** — barre de transport, liste des pistes, piano roll et sa barre, mixeur, voies de CC, de tempo et d'automation, arrangement, liste d'événements, rack, spectre — sont traduits, infobulles comprises, et suivent la bascule (D94 : chaînes françaises atteignant l'écran sans `tr()` 396 → 273, compte de `tools/inventaire_langue.py` ; textes identiques FR / EN 146 → 103, tous neutres ou données sauf quatre noms de machines ; français identique au témoin, liste et image — hors la charge CPU). **Les boîtes du montage** — piste verrouillée, jonction ou changement de piste refusés, transposition hors plage, tempo d'une boucle, et les actions d'un clip audio (découper, rogner, transcrire, créer) — sont traduites (D95 : les 47 chaînes du lot ; une boîte se lit au moment où elle est demandée, `VSM_BOITE`, parce qu'une boîte ouverte au démarrage d'un banc n'est plus là à la photo sous un écran verrouillé ; français identique au témoin). **Les opérations de piste** — geler, reporter la piste ou la sélection, éclater par hauteur, publier les sorties — répondent dans la langue de l'interface, et sous le nom de leur menu (D96 : « Split by pitch » partout où « Explode by pitch » était ; français identique au témoin). **Les boîtes du projet** — statistiques, modèle, ouverture et enregistrement, import et export MIDI, audio non chargé, locateurs, titres des sélecteurs de fichier — sont traduites (D97 : −42 à l'inventaire, vérifiés sans faux TABLE ; les statistiques traduites à la source, parce que personne ne relit leur ligne au terminal). **Le groove et les presets de piste** répondent dans la langue de l'interface (D98 : −17, vérifiés sans faux TABLE). **Le navigateur** aussi (D99 : −18, huit affichages de plus que l'inventaire ne voyait pas, et les erreurs JSON et WAV qu'il montre ; commande de banc `VSM_NAVIGATEUR`). **Les quinze fenêtres flottantes** ont leur titre dans la langue, reposé au changement de langue, sans perdre la position retenue sous leur titre français (D100). **Les plugins** aussi (D102 : −21 à la règle stricte, −30 à la large ; commande de banc `VSM_PLUGIN`, et les fenêtres de choix lues quand elles sont demandées, `VSM_CHOIX`). **Les noms des machines** aussi, dans la liste des pistes, le rack et le navigateur, et ils suivent la bascule (D103 : 47 clés ; `--machines` compte les noms sans clé, 48 → 0 ; « Sampler (8 emplacements) » était faux, il en a 16). **La reprise après une session interrompue** aussi — la boîte qu'on lit quand le logiciel vient de tomber (D104 ; `demanderOuiNon()` lit une question à deux boutons quand elle est demandée). **Les dernières boîtes des opérations de piste** — prise retirée, signature, report en audio, import audio, ce qui vient d'être joué — aussi (D105 ; 16 messages de la même série ne vont qu'au terminal : l'inventaire suit désormais la phrase jusqu'à son `fputs`, D106). **Les boîtes du démarrage** (raccourcis et associations MIDI illisibles), l'import audio dans un projet jamais enregistré, les titres des sélecteurs de fichier et le menu des cibles d'une association MIDI aussi ; **une piste ajoutée prend son nom dans la langue de l'interface** (« Track 2 »), qu'elle garde ensuite (D107). **L'original de l'écoute A/B** aussi — la boîte d'un enregistrement illisible, les erreurs du décodeur (qui retrouvent leurs accents), la ligne du menu Fichier (D108 ; commande de banc `VSM_FICHIER`). **Les noms des pièces de batterie** aussi — sur le clavier du piano roll, et dans le nom des pistes qu'« Éclater par hauteur » crée (D109). **Les boîtes du départ d'une prise** aussi — deux pistes audio armées, projet jamais enregistré — et les erreurs du moteur audio et du rédacteur de prise qu'elles montrent, qui retrouvent leurs accents (D110 : 1 boîte sur 1 dans chaque langue, français identique au témoin ; geste de banc `VSM_GESTE_PISTE=armer`). **L'erreur d'« Aller à la mesure » et les deux boîtes d'« Assembler les prises »** aussi, et les réserves d'effet d'un bus de départ — déjà traduites à l'affichage depuis D89, mais composées par `+` dans le source, si bien que l'inventaire les comptait encore (D111 : donnée et affiché identiques au témoin octet pour octet, dans les deux langues). **Les boîtes de l'enregistrement** aussi — file de capture débordée, mesure de latence (pas d'entrée, rien de retrouvé, résultat), disque trop lent —, qu'aucun banc n'atteignait sans jouer de son : une fonction chacune, montrée par le geste de banc `VSM_BOITE_ESSAI` avec des chiffres fixes (D112 : 5 boîtes sur 5 par langue, mêmes nombres en anglais et en français). **Les raisons du balayage des plugins** aussi, et celles des hôtes CLAP (D113, voir A12). **La reconstruction lancée depuis l'application** aussi — « Reconstruction indisponible » et ses raisons (écrites par `ReconstructionChain`, traduites à l'affichage), le volet, le lanceur, le transcripteur, le texte d'attente des notes (D114 : la boîte par `VSM_BOITE_ESSAI=indisponible`, deux cas, raison vraie ; le volet et le lanceur vérifiés par le code) ; et dans la boîte du dépôt d'un fichier, où elles restaient françaises (D124, lu sur `VSM_BOITE`). **À l'inventaire strict, il reste 10 chaînes, et aucune ne s'affiche** — relevé du 18/09 ; **7 le 11/09** : D337 en a ajouté trois (les phrases de son journal, assemblées dans une variable locale avant leur `fputs`, que l'inventaire compte alors comme de l'écran — il suit la phrase jusqu'à sa sortie, D106, et perd la trace à la frontière d'une variable). Les sept autres sont vérifiées une à une : une étiquette de nature du relevé `VSM_TEXTE`, un mot CHERCHÉ dans une ligne (« non appliqué »), trois préfixes de nom de fichier, un nom de fil d'exécution (D113) et une clé de groupe de menu pour `VSM_MENU_CONTEXTE`. **Ce que le compte strict ne voyait pas** non plus : « RÉGLAGES », la seule des 163 sérigraphies de section de façade qui ne soit pas anglaise (elle vit dans `panels/`, hors du champ de l'inventaire), et deux phrases d'état du piano roll de l'angle mort (D115 : « SETTINGS », « 12 note(s) softer than 64 » ; geste de banc `VSM_MENU_CONTEXTE=pianoroll:…`). **L'angle mort de l'inventaire** est mesuré (D101) : la règle stricte laisse passer au moins 33 chaînes françaises, nommées — ÉCRAN réel ≥ 159 à D101, ≥ 55 après D108 (ÉCRAN 45 à l'inventaire, 10 chaînes de l'angle mort) ; `--regle=large` les montre, avec 20 faux (unités, mots anglais). **Reste** : `MainComponent` hors du montage, des pistes, du projet, du groove, du navigateur et des plugins (5 chaînes à l'inventaire depuis D114 — 34 avant D110 —, aucune ne s'affiche : un jeton de banc, le mot que le volet cherche pour colorer, trois chemins de fichiers), ~~10 des 33 chaînes de l'angle mort~~ (*toutes traitées au 11/09 — D102 à D115 —, ou ne s'affichant pas : les deux « restaurations de preset » de D101 vont au terminal seul*), ~~un titre « Projet jamais enregistré » écrit sans `tr()`~~ (traduit par D110, le dernier des trois faux TABLE de D96), ~~des infobulles françaises écrites sans leurs accents~~ (*D118 : 60 infobulles distinctes relevées à l'écran en anglais, aucune française ; panneaux fermés, façades et boîtes non couverts* — *D128 : les panneaux fermés et six fenêtres flottantes relevés, 1 infobulle de plus et 53 textes de plus, aucun français ; restent non lues les façades, la fenêtre des tronçons (qui ne s'ouvre pas sans prise) et la fenêtre flottante de l'arrangement* — *D129 : les façades des 64 machines compilées relevées à l'écran, 357 infobulles distinctes, aucune française ; reste la fenêtre de l'arrangement* — *lue par D130 : elle dessine tous ses textes, anglais sur la photo* — *rectifié par D130 : « la fenêtre des tronçons, qui ne s'ouvre pas sans prise » était une supposition ; le jeton `troncons` n'est qu'une sonde au terminal, et le panneau des tronçons (« Comp takes ») a été lu par D128*) ; ~~les couches audio, plugins et reconstruction (15)~~ (*au 11/09, l'inventaire strict n'y trouve plus que le nom du fil de balayage des plugins, qui ne s'affiche pas — D110, D113, D114*) ; ~~les réserves d'un preset appliqué à la main~~ (traduites depuis D89-D90 — VÉRIFIÉ au banc par D116, un preset abîmé appliqué en anglais) ; ~~le message d'import audio~~ (D105) ; ~~les raisons pour lesquelles la reconstruction est indisponible~~ (traduites à l'affichage par D114) ; ~~« RÉGLAGES », titre de section des façades~~ (D115) ; deux textes aux retours à la ligne forcés (D91) — *D117 : la boîte des mesures d'un clip n'a plus de mot orphelin ; le renommage garde le motif entier mais coupe encore l'exemple de résultat : `AlertWindow` choisit sa largeur d'après le texte, et trois gestes mesurés n'y suffisent pas* — *clos par D121 : les deux boîtes passent par `BoiteLisible`* ; ~~la boîte « Chaîne d'analyse »~~ (un dossier de chaîne invalide ; titre, raison et remède restaient français — un faux TABLE de l'inventaire, trouvé par D125 —, *traduits par D126, avec les deux lignes « ↳ » du menu Fichier qui montrent la même raison*) |
| ~~A12~~ | ~~Un plugin illisible disparaissait du balayage sans un mot~~ — **CLOS par D113**, et c'était trois défauts : l'erreur de l'hôte jetée par `ignoreUnused`, un enfant tué par un signal que `getExitCode()` de JUCE lit « 0 », et un délai de 20 s que la lecture bloquante du tube rendait lettre morte. L'enfant écrit dans un fichier et finit par une sentinelle | `ROADMAP-daw.md` D113 | 4 faux `.clap` : **0 → 4** fautifs nommés ; balayage **61 s → 22 s** |
| ~~A13~~ | ~~Trois boîtes sur dix coupent mal à 150 %~~ — **CLOS par D121 et D124** : `BoiteLisible` met le texte d'une boîte que l'application construit dans un bloc que JUCE met en page APRÈS avoir fixé la largeur, et une cale impose la largeur de la ligne la plus longue (D121, deux boîtes) ; « Que faire de ce fichier ? », une boîte STATIQUE de JUCE où « minutes. » restait seul, y passe aussi, avec sa variante à deux boutons (D124). Les autres boîtes statiques de l'application n'ont pas été relevées par D119 : pas réputées bien coupées, elles le seront quand un audit les montrera | `ROADMAP-daw.md` D117 → D121 → D124 | 4 photos sur 20 → **1** (D121) → **0** (D124) |
| ~~A14~~ | ~~Chaque fenêtre en plein écran, et retour, d'un simple clic~~ (demande de l'utilisateur, 11/09) — **FAIT par D122** : les quinze fenêtres flottantes annoncent agrandir au gestionnaire de fenêtres (`0x2c` → `0x3c`), une fenêtre agrandie ne retient pas ses limites ; les quatre zones de la fenêtre unique ont un bouton dessiné, agrandir et rendre ; la fenêtre principale avait déjà F11 | `ROADMAP-daw.md` D122 | retour **0 pixel** ; limites retenues inchangées |
| ~~A15~~ | ~~Les boîtes statiques de JUCE — 113 sites — coupent rarement mal, et dessinent leur texte par-dessus l'icône~~ — **CLOS par D126 et D127** — D125 : douze boîtes de la famille photographiées en français et en anglais, une seule mauvaise coupure (« it. » seul dans « The disk did not keep up ») ; JUCE garde, faute d'équilibre à 10 % près, la largeur où les deux dernières lignes sont les PLUS déséquilibrées. **« it. » corrigé par D126** (la boîte par `BoiteLisible`, dont la cale ne franchit pas le plafond de JUCE — 896 px ici : la phrase française y repasse proprement à la ligne). Le texte par-dessus l'icône est un FILIGRANE voulu par JUCE (40 % d'opacité), et il ne nuit pas à la lecture : contraste du texte **6,84:1** sur le « i », **9,19:1** sur le « ! », au-dessus du seuil AA (4,5:1) — le style reste (D127) | `ROADMAP-daw.md` D125 → D126 → D127 | français **0 / 12**, anglais **1 / 12 → 0** ; contraste au pire **6,84:1** |
| ~~A16~~ | ~~L'afficheur de valeur des façades colle « : » en anglais~~ — **CLOS par D132** : un geste de banc d'abord (`facade:<légende>=<valeur>`, la même notification que la souris), qui a montré le défaut au témoin, puis l'afficheur par `deuxPoints()` | `ROADMAP-daw.md` D131 → D132 | « CUT OFF FREQ : 1200 Hz » → **« CUT OFF FREQ: 1200 Hz »** en anglais ; français identique |
| ~~A17~~ | ~~L'afficheur de valeur d'une façade est hors de la vue quand on tourne un bouton~~ — **CLOS par D133** : l'afficheur sort de ce qui défile, une bande fixe au pied du rack, la façade hébergée cachant le sien ; sur la photo dans les dispositions par défaut et flottante, un seul libellé. *Constat d'origine* — vu sur la photo de D132 : il est posé au bas de la façade, sous le séquenceur, et dans la disposition par défaut le rack coupe la façade du TB-303 à l'en-tête « PATTERN ». Le relevé des textes le lit (`isVisible()`), l'écran ne le montre pas. Le musicien règle sans voir la valeur, sauf à faire défiler | `ROADMAP-daw.md` D132 → D133 | afficheur absent de la photo → **visible** au pied du rack (défaut et flottante) |
| ~~A18~~ | ~~Quatre familles de commandes se règlent sans voir leur valeur~~ — **CLOS par D135** : une bulle de valeur (posée dans la fenêtre du curseur), mise en forme lisible — « -2.5 dB », « C », « 0.0 dB », « 2.0:1 », « 1.00 » vus sur photo ; les départs vérifiés au code ; le panoramique de ligne de piste n'a pas de surface au dock par défaut (A19). *Constat d'origine* — le volume et le panoramique de chaque ligne de piste, le panoramique et les départs de chaque tranche du mixeur, les sept boutons MASTER (dont l'unité existe et n'est jamais montrée), tous les paramètres du panneau générique du rack. Aucune bulle de valeur dans l'application ; Cubase et Live en montrent une pendant le réglage | `ROADMAP-daw.md` D134 → D135 | 0 bulle → **bulle** sur les 4 familles ; 5 cas vus sur photo |
| ~~A19~~ | ~~Au dock par défaut, six réglages de la ligne de piste ont une largeur nulle~~ — **CLOS par D136** : une disposition étroite en quatre rangées (120 px), rien de caché ; la disposition large inchangée au pixel. *Constat d'origine* — canal, muet, solo, armement, panoramique, sortie (D136 : chaque rangée pose ses éléments à largeur fixe, de gauche à droite, pour ~150 px utiles ; l'armement et la sortie ne sont pas dans le mixeur). Trouvé par le banc de D135 : la troisième rangée donne 170 px au volume, puis 90 au panoramique et 130 au sélecteur de sortie, et le dock des pistes en fait ~180 ; le panoramique reçoit une largeur NULLE (le geste de banc l'a trouvé « visible » et l'a mis à fond à gauche en l'appuyant). La famille de D123 (« + Ajouter une piste » écrasé) : une disposition qui suppose une largeur que le dock n'a pas | `ROADMAP-daw.md` D135 → D136 | 6 réglages à 0 px → **tous posés** ; large : 0 pixel de différence |
| ~~A20~~ | ~~Au dock des pistes le plus étroit (180 px, le minimum permis), le nom de la sortie d'une ligne ne se lit pas~~ — **CLOS par D137** : l'infobulle de la liste de sortie nomme la destination (« Output: Master — … », « Sortie : Batterie — … »), reposée au changement ; le nom abrégé se lit au survol, sans cacher de réglage ni réécrire la largeur de l'utilisateur. *Constat d'origine* (« -… ») — le reste de D136, BORNÉ par une mesure : au défaut du code (300 px), la disposition étroite se lit entièrement (« TB-303-style Acid Synth », M S R, « -> Master ») ; à 180 px, ~150 px utiles ne tiennent pas trois boutons ET le nom d'un bus. Les préférences de l'utilisateur sont à 180 : c'est ce qu'il voit. La disposition est réglable, et on ne réécrit pas sa largeur | `ROADMAP-daw.md` D136 → D137 | sortie : « -… » à 180 px ; lisible à 300 ; **destination nommée au survol** |
| ~~A21~~ | ~~Aucun réglage de machine ne s'annule~~ — **CLOS par D154** : le modèle porte la table de la machine (`ISynthPlugin::saveState`, `ParamId → valeur`, jamais écrite au fichier), photographiée à chaque pas d'annulation comme le MASTER depuis D144, et reposée à la reconstruction sur les machines GARDÉES — le trou que D76 laissait ouvert. Mesuré sur le TB-303 du projet de D91 : 1200 Hz, Ctrl+Z → **480 Hz** (la valeur du projet, pas celle d'usine — c'est le témoin sans geste qui l'a dit, et mon attendu qui était mal écrit), Ctrl+Maj+Z → 1200 ; annuler un geste de PISTE laisse la machine intacte. Coût de la photo : **36 µs au pire** sur neuf machines, pour une borne écrite à 5 ms. *Constat d'origine* — trouvé en vérifiant D140 : double-clic puis Ctrl+Z, le volume de la ligne et le fader reviennent (-0.9 dB), la coupure d'une façade reste à 800 Hz ; un glissé sur une façade ne s'annule pas davantage (son rappel envoie la valeur au synthé sans ouvrir de pas d'annulation). Ces réglages font partie du projet (`instruments/track_NN.synth.json`) ; D36 a compté les champs de la piste qui échappaient à l'annulation, pas les réglages de machine. **DÉBLOQUÉE LE 12/09 À 12:29**, l'épreuve Children terminée (code 0 sur ses trois courses, `CDC-detection-multipiste.md` § 12.4) : c'est l'élément suivant de la feuille de route. *Ce que le blocage disait* : les réglages vivent dans les instances des synthés, capturés (`capturePreset`) seulement à l'enregistrement ou quand une machine est recréée (D76) ; un pas d'annulation copie le MODÈLE, qui ne les porte pas. Le remède fait porter les réglages par le modèle — `core/` et `interchange/`, que la chaîne compte dans l'empreinte du moteur : les toucher avant la course 3 la ferait partir sur un autre moteur que la course 1, ou pas du tout. Un contournement dans l'application seule (une pile parallèle calée sur l'historique) serait fragile, et n'est pas retenu | `ROADMAP-daw.md` D140 → D154 | réglage de façade + Ctrl+Z : 800 Hz → 800 Hz (le défaut) ; après : 1200 → **480** (annulé), et rétabli à 1200 |
| ~~A22~~ | ~~À l'ouverture, la tranche MASTER montre les valeurs d'usine au lieu de celles du projet~~ — **CLOS par D142** : le MASTER du projet est appliqué avant que le mixeur ne le relise ; bouton 6.0 dB = moteur 6.00, et le double-clic ramène le moteur à 0.00. *Constat d'origine* — mesuré par D141 : LOW enregistré à +6 dB, le moteur a 6.00, le bouton affiche 0.0 dB. Cause : dans `rebuildFromProject`, le mixeur relit le MASTER dans le moteur (`mixer_.setProject`) AVANT que le MASTER du projet y soit appliqué. Conséquence : un double-clic « remettre à zéro » ne fait rien (le bouton est déjà à 0), pendant que +6 dB s'applique ; l'affichage ne se rattrape qu'à la reconstruction suivante (une annulation). Se corrige dans l'application seule. Ensuite seulement se mesurera le soupçon de D141 (une annulation qui ramène le MASTER au dernier enregistrement) | `ROADMAP-daw.md` D141 → D142 | bouton 0.0 dB / moteur 6.00 → **6.0 dB / 6.00** |
| ~~A23~~ | ~~Annuler un geste de piste ramène le MASTER au dernier enregistrement~~ — **CLOS par D144** : chaque pas d'annulation porte le MASTER de son instant (recopié au début du pas et avant chaque annulation ou rétablissement), et les gestes du MASTER ouvrent leur pas. *Constat d'origine* — mesuré par D143 : LOW enregistré à +6 dB, remis à 0 par le musicien, puis Ctrl+Z sur un geste de volume de piste : le moteur revient à 6.00, sans un mot. La photo d'un pas d'annulation porte le MASTER du dernier enregistrement (`masterParameters` n'est rafraîchi qu'à l'enregistrement, l'export, le gel, le report) et `rebuildFromProject` la réapplique ; les gestes du MASTER, eux, n'ouvrent aucun pas. Une panne muette. Se corrige dans l'application seule | `ROADMAP-daw.md` D141 → D144 | annuler un geste de piste : moteur 6.00 → **0.00** (le réglage gardé) ; annuler le geste du MASTER : 0.00 → **6.00** |
| ~~A24~~ | ~~L'activation du bus master ne s'annule pas~~ — **CLOS par D146** : le clic ouvre son pas AVANT la bascule (`beginProjectEdit("Master")`, qui depuis D144 photographie le MASTER du moteur) puis recopie le moteur dans le modèle ; Ctrl+Z rend l'activation (1.00), une annulation reprend UN geste et deux en reprennent deux (0.00 → 1.00 à la seconde). Témoin pris juste avant le correctif, deux séries d'après identiques. *Constat d'origine* — mesuré par D145 : « Master Enabled » enregistré à 1, un clic sur le bouton MASTER l'éteint (moteur 0.00), et Ctrl+Z le laisse éteint ; un geste de piste intercalé n'y change rien, puisque depuis D144 le pas photographie le MASTER du moteur — donc l'activation DÉJÀ éteinte. C'est un paramètre du projet (`project.json`, « Master Enabled », `MasterBus.h:56`), à la différence de l'écoute mono, qui survit à l'annulation comme elle le doit (D23.5, vérifié au même banc : mono 1, zéro clé mono au relevé). Cause : `onMasterEnable` (`MainComponent.cpp:325`) pose la valeur dans le moteur sans ouvrir de pas ni prévenir le modèle — le défaut de D143, pour le seul rappel que D144 n'a pas repris. Se corrige dans l'application seule | `ROADMAP-daw.md` D145 → D146 | clic MASTER puis Ctrl+Z : moteur 0.00 → **1.00** |
| ~~A25~~ | ~~L'armement d'une piste ne s'annule pas~~ — **CLOS par D148** : le bouton ouvre son pas avant d'écrire (`debutEdition("Armement")`, le rappel d'une rangée étant déjà câblé à `beginProjectEdit`), et un seul point corrigé couvre les deux chemins, le verbe `armer` de D110 passant par le même `onClick` ; Ctrl+Z efface la marque « R », Ctrl+Maj+Z la rétablit, et le muet, le solo et le contrôle ne bougent pas. Trois séries de témoin, deux d'après, identiques. *Constat d'origine* — mesuré par D147 : armer la piste au BOUTON (« R ») puis Ctrl+Z laisse la marque en place, alors que le muet et le solo de la même ligne s'effacent, au verbe comme au bouton. Contrôle dans la même série : le même `cliquer:` sur le bouton voisin (muet) efface bien sa marque, donc c'est l'armement qui est en cause, pas le geste. Cause : `armButton_.onClick` écrit `track_.armed` et n'appelle que `refreshArmedTracks()` (`MainComponent.cpp:598`) — ni pas d'annulation, ni photo —, alors que `Track::armed` EST un champ du projet (`Track.h:591`), sauvé et rechargé. À la différence de l'écoute mono, qui n'est PAS dans le fichier et reste à bon droit un outil de séance (D23.5). Se corrige dans l'application seule, par le chemin de D146 | `ROADMAP-daw.md` D147 → D148 | armer puis Ctrl+Z : « R » → **effacée** ; Ctrl+Maj+Z : **« R »** |
| ~~A26~~ | ~~La boîte des réglages audio est entièrement en anglais~~ — **CLOSE par D159 le jour même** : une table de l'anglais vers le français est posée quand la langue est le français, et **9 des 12 textes** sont traduits (« Taux d'échantillonnage : », « Taille du tampon audio : », « Joue un son d'essai »). Les trois autres sont nommés et ne peuvent pas l'être : un nom de périphérique du système, une valeur, et « 512 samples (11.6 ms) » que JUCE compose SANS `TRANS`. *Constat d'origine* — douze textes relevés, tous anglais (« Sample rate: », « Audio buffer size: », « Plays a test tone »), dans l'interface française. C'est le composant de JUCE, que l'inventaire de langue NE PEUT PAS voir : il compte les chaînes de `app/Source`, et celles-ci n'y sont pas. Un musicien français règle sa carte son en anglais. Le remède est `juce::LocalisedStrings`, qui traduit les composants de JUCE eux-mêmes | `ROADMAP-daw.md` D158 | 12 textes sur 12 en anglais |
| ~~A27~~ | ~~Ajuster à la fenêtre fait tomber le dessin à 21 images par seconde~~ — **CLOS par D165**, et le premier remède était à côté : D163 a mesuré 46,86 ms pour la fenêtre entière après `Ctrl+0` sur `children-c3-plafond` (12 pistes, 9 224 notes, 452 s ; témoin projet vide 10,09 ms), et l'A/B par panneau a mis tout le surcoût sur le piano roll (2,56 → 30,98 ms). **D164 a accusé les rectangles de notes et s'est trompé** : chronométrées, les six étapes de `PianoRollComponent::paint` ne font que 1,6 ms — les NOTES 0,07 ms — quand le panneau en coûte 30. L'instrument a donc été étendu (`VSM_PEINTURE_ENFANTS`, chaque enfant nommé par sa classe), et il a désigné les vrais postes : `VelocityLaneComponent` **18,06 ms** et `PianoRollRulerComponent` **8,29 ms**. **D165** : la règle ne numérote qu'une mesure sur N (34 px entre deux numéros) et ne trace ses traits qu'au-delà de 3 px — elle empilait 226 numéros dans 560 px, illisibles ; la voie de vélocité ne trace plus un contour d'un demi-pixel qui recouvrait la moitié d'une barre de 3 px et en mangeait la teinte. Le rectangle plein de D164 est gardé : il divise par deux le dessin du piano roll lui-même. Témoin au pixel : **0 différence sur 212 800** pour le panneau au zoom d'ouverture, 42 pixels sur la fenêtre entière — le seul afficheur de charge CPU | `ROADMAP-daw.md` D163 → D164 → D165 | règle **8,29 → 0,08 ms** (× 104), vélocité **18,06 → 2,76**, fenêtre ajustée **46,86 → 21,95 ms** (21 → 46 im/s) |
| ~~A28~~ | ~~Au repos, le logiciel brûle un quart de cœur à redessiner ce qui n'a pas bougé~~ — **CLOS par D169** : **23,30 % d'un cœur** fenêtre ouverte, transport à l'arrêt, projet de 12 pistes (et **22,25 % sur un projet VIDE** — ce n'était pas le contenu qu'on redessinait, c'était la surface), dont **21,45 % sur le fil des messages** au relevé par fil d'exécution. Trois minuteurs poussaient un redessin inconditionnel : `PianoRollComponent::setPlayheadTick` appelait `repaint()` sans comparer le tick, là où `ArrangementComponent` garde depuis toujours (D168, −4 points) ; puis `PianoRollPanel::refresh()` repeignait la règle et `PianoRollToolbar::refreshFromPianoRoll()` finissait par un `repaint()` nu (D169, −13,75 points). Les quatre autres minuteurs ont été relus : tous gardaient déjà leur affichage. **Reste 5,50 %**, soit 0,5 point au-dessus de l'attendu écrit d'avance — c'est le travail du minuteur de 30 Hz, pas un dessin, et le minuteur reste à 30 Hz pour la fluidité de la tête de lecture. Vérifié que la tête bouge encore : deux captures pendant une lecture diffèrent de 2 244 pixels | `ROADMAP-daw.md` D167 → D168 → D169 | **23,30 % → 5,50 %** d'un cœur au repos (× 4,2) |
| ~~A29~~ | ~~L'application ne dit jamais qu'un projet a des modifications non enregistrées~~ — **CLOS par D174** : trouvé en éprouvant l'enregistrement DANS UN DOSSIER EN LECTURE SEULE (D173), qui lui se plaint bien (`VSM_BOITE : Enregistrement impossible…`, fichiers intacts au `md5sum`). Le titre ne portait que le nom du dossier, et le seul drapeau existant, `projectDirty_`, sert à l'autosauvegarde **qui le remet à faux toutes les 30 s**. Le titre porte désormais une astérisque, déduite de l'écart entre la profondeur d'annulation courante et celle du dernier enregistrement réussi — déduire plutôt que déclarer, la leçon que l'autosauvegarde avait payée quatre fois. Instrument neuf `VSM_TITRE` : le titre d'une fenêtre n'est le texte d'aucun composant, ni l'autoportrait ni `VSM_TEXTES_LISTE` ne le voient. Quatre cas mesurés, dont celui qui compte : un enregistrement REFUSÉ laisse la marque | `ROADMAP-daw.md` D173 → D174 | ouvert 0 marque ; après un geste **`*`** ; après Ctrl+S réussi, tombe ; après Ctrl+S refusé, **reste** |
| ~~A30~~ | ~~Quitter ne demande rien~~ — **CLOS par D175** : `systemRequestedQuit()` appelait `quit()` sans un mot (`Main.cpp:173`), et fermer la fenêtre avec des modifications non enregistrées les abandonnait (filet : autosauvegarde à 30 s et boîte de reprise de D104). La fermeture pose maintenant une question à trois réponses ; *Enregistrer* ne quitte QUE si l'écriture réussit — sinon la boîte de D173 s'affiche, la marque de D174 reste et l'application reste ouverte. La précaution qui sauve l'outillage : les trois sorties du bloc `VSM_CAPTURE` appellent désormais `quit()` directement (une fin de course de banc n'est pas un geste d'utilisateur), sans quoi tous les bancs du dépôt auraient expiré ; et `VSM_FERMER=1` déclenche la vraie fermeture avant la photo pour que la question reste vérifiable | `ROADMAP-daw.md` D174 → D175 | modifié : **la question et ses trois réponses** ; non modifié : **aucune boîte** ; témoin de banc : la course se termine, 41 pixels d'écart, tous le nom du dossier |
| ~~A31~~ | ~~Une piste audio qui ne charge pas n'entre pas dans le rapport d'ouverture~~ — **CLOS par D180** : trouvé en ouvrant un projet reconstruit privé d'un de ses `samples/*.wav` (D179). L'avertissement existait — une boîte « Audio non chargé » nommant la piste et le fichier, et l'enregistrement ajoute même « Projet incomplet » — mais il ne vivait QUE dans cette boîte : le volet « Projet ouvert, avec des réserves », qui est la mémoire de l'ouverture et qui se rouvre, comptait une seule réserve (les notes douteuses). Une fois la boîte fermée, plus rien ne disait pourquoi la piste était muette. Les pistes illisibles prennent maintenant le chemin des réserves d'effets (D71) : un membre assigné — non empilé, `loadAudioTracks()` étant rappelée à chaque pas d'annulation — recopié dans le rapport. *À l'aller, le premier relevé filtrait `VSM_OUVERTURE` et `VSM_RAPPORT` mais pas `VSM_BOITE` : j'ai failli publier « l'application ouvre sans un mot » alors qu'elle disait tout dans la ligne jetée par mon filtre — la règle de D147, repayée* | `ROADMAP-daw.md` D179 → D180 | volet : **1 réserve → 2** ; témoin projet intact : **1**, inchangé |
| ~~A32~~ | ~~Geler une piste la fait disparaître de l'export~~ — **CLOS par D186** : `children-c3-plafond`, piste `bass` gelée par le menu, le mixage exporté bouge de **6,55 dB** (corrélation 0,882) et la piste sort « **01 - bass.wav : silencieux** » en stems. Ce n'est pas un désaccord entre les deux moteurs — l'export de l'application et `vsm-render` sont identiques au plancher du 24 bits : c'est le MÊME défaut dans les deux, le rendu hors ligne ne branchant l'audio que pour `kind == Audio` et depuis `track.audio`, alors qu'une piste MIDI gelée garde `kind == Midi` et met son audio dans `frozenAudio` — et son instrument est sauté à bon droit. Aucun avertissement n'était émis. Le chemin TEMPS RÉEL, lui, faisait l'inverse : **ce qu'on entendait n'était pas ce qu'on exportait**, l'invariant n° 3 du § 6 par terre pour les pistes gelées. Le hors ligne prend les quatre lignes du temps réel | `ROADMAP-daw.md` D185 → D186 | gelé contre non gelé : **−6,55 dB → −52,79 dB**, puis −∞ avec A33 |
| ~~A33~~ | ~~Un gel coupe la queue de la piste, net~~ — **CLOS par D187** : trouvé dans le reste de D186, dont l'écart valait ZÉRO de 0 à 450 s et **−19,79 dB sur les quatre dernières secondes**. Le fichier de gel faisait **451,735 s** pour un projet de **454,164 s** — 2,43 s manquantes — et se terminait sur un dernier échantillon à **−0,17**, une coupure nette au milieu du son : `renderTrackForFreeze` isole la piste puis laisse le rendu déduire sa durée de cette piste SEULE, alors qu'une machine qui tient (vielle à roue, pad, bourdon) sonne jusqu'à la fin du morceau. La durée du gel est désormais celle du PROJET. Résultat : **corrélation 1,000000000, écart maximal 0,000 sur un échantillon**, −5 980 dB par tranche de 60 s, fin comprise | `ROADMAP-daw.md` D186 → D187 | gel **451,735 s → 454,164 s** ; geler ne change plus RIEN au son exporté, **au bit** |
| ~~A34~~ | ~~Le profil d'une machine multi-échantillons n'est jamais capturé — l'enregistrement l'EFFACE et l'export perd la piste~~ — **CLOS par D189**, et c'est l'anomalie la plus grave de la série parce qu'elle abîme le fichier. Trouvée en comparant les deux chemins de rendu sur le morceau que l'utilisateur avait demandé (`b4wuzthen`, D188) : corrélation **0,937**, écart **−8,61 dB**, et la différence corrélée à **0,9999** avec le seul stem `other` — la piste qui joue `vsm.multisample` sur le patch FR3-Accordion, dont les échantillons vivent HORS du projet. `capturePreset` — par où passent l'export, le gel, le report ET l'enregistrement — capturait les paramètres et, depuis D76, les échantillons d'un sampler à emplacements, mais jamais le PROFIL d'une machine à profil : le champ existait, le JSON l'écrivait, `applyPresetSamples` le reposait, la machine savait le dire (`IMultisampleBank::profileName()`) — personne ne le demandait. **Un simple Ctrl+S effaçait « profile: FR3-Accordion » du fichier**, sans un mot | `ROADMAP-daw.md` D188 → D189 | Ctrl+S : profil **effacé → conservé** ; export contre `vsm-render` : **0,937 / −8,61 dB → 1,000000000 / écart maximal 0,000** |
| ~~A35~~ | ~~Le même projet s'ouvre dans deux fenêtres, en silence~~ — **CLOS par D195** : `moreThanOneInstanceAllowed()` rend vrai à bon droit (comparer deux projets côte à côte), mais rien n'empêchait d'ouvrir LE MÊME dans les deux — chacune sa copie en mémoire, un seul dossier sur le disque, aucun arbitrage : la dernière qui enregistre écrase l'autre, et rien n'avait prévenu (D194 : la seconde ouverture ne disait qu'une réserve, celle des notes douteuses). Un `juce::InterProcessLock` sur le chemin du projet, **et non un fichier de verrou écrit à la main** : il se libère à la mort du processus, `kill -9` compris ; il ne touche pas au dossier (un projet en lecture seule s'ouvre quand même) ; il est le même sur les trois systèmes. **On prévient, on ne refuse pas** — regarder un projet ouvert ailleurs est légitime. Quatre cas mesurés, dont les deux témoins : une instance seule qui rouvre son projet et deux projets différents côte à côte ne disent rien | `ROADMAP-daw.md` D194 → D195 | 2 instances, même projet : **silence → la boîte, dans les deux langues** |
| ~~A36~~ | ~~Un projet qui utilise un plugin TIERS ne rouvre jamais avec lui~~ — **CLOS par D200** : mesuré sur le `Juno 106.vst3` installé sur cette machine (D199) — il se charge, joue, son état natif (1 384 octets) s'enregistre, mais à la réouverture la piste était déclarée **indisponible** alors que le plugin était installé, balayé et catalogué au même chemin et au même identifiant. La cause tenait en une ligne : `pluginIsInstalled()` appelait `PluginRegistry::isRegistered()`, qui ne regarde que les 63 machines internes — son en-tête dit pourtant « ce qui décide reste `create()` ». `canCreate(id)` est née pour cette question-là : elle demande l'instance au résolveur externe et la jette, et son coût est écrit avec l'endroit où l'appeler. Le témoin fait la moitié du correctif : un plugin VRAIMENT absent continue d'être dit, sans quoi « accepter tout » aurait rendu muette une machine manquante | `ROADMAP-daw.md` D199 → D200 | réouverture : **« indisponible » → « Machines employées : 5 », le vst3 compris** |
| ~~A37~~ | ~~Les cinq entrées qui remplacent le projet ne demandent rien~~ — **CLOS par D211** : `demanderAvantDeQuitter()` (D175) n'avait qu'UN appelant, `systemRequestedQuit()`, alors que « Nouveau projet », « Ouvrir MIDI… », « Ouvrir un projet VSM… », « Nouveau depuis le modèle » et « Reconstruire un morceau… » appellent toutes `clearHistory()` puis réaffectent `project_`. Mesuré avant : une piste ajoutée, puis « Nouveau projet », et le travail disparaissait sans une ligne `VSM_BOITE`. La question est maintenant commune (`demanderAvantDeJeter`), posée AVANT le sélecteur comme dans Cubase, et le banc y répond par `VSM_ABANDON=abandonner`, `annuler` ou `enregistrer` | `ROADMAP-daw.md` D211 | avant : **0 boîte** sur 3 entrées pilotables ; après : la question sur les 5, et la branche *Enregistrer* réécrit `project.json` (`a7052f26…` → `37ef71bc…`, la piste ajoutée dedans) AVANT de remplacer |
| ~~A38~~ | ~~Le titre de la fenêtre garde le nom du projet PRÉCÉDENT après « Nouveau projet »~~ — **CLOS par D212** : `newProject()` posait `project_.title` (le nom dans le fichier) et jamais `titreDeBase_` (le nom de la FENÊTRE, le seul qui se voie) ; la marque « non enregistré » se comparait en outre à la profondeur du dernier enregistrement de l'ANCIEN projet. Trouvé par le TÉMOIN de D211, posé pour une autre question. Même correction dans `ouvrirLeMidi()`, dont le verbe de banc recopiait le cœur (piège de D202) | `ROADMAP-daw.md` D212 | avant : « -- children-c3-plafond » au-dessus d'un projet vide ; après : « -- nouveau projet » (« -- new project » en anglais) et « -- arrangement » après un MIDI ouvert |
| ~~A39~~ | ~~**« Note douteuse » désigne 53 à 91 % des notes** — le seuil `kDoubtfulNoteThreshold = 0,55` a été posé « dans les cas mesurés » qui n'étaient pas ceux-là : relevé sur six reconstructions, il marque 53 % de `cdl`, 73 % de `b4wuzthen`, 91 % de `sky-parite`. La touche D, qui mène « une par une », propose donc 2 600 notes. **À MOITIÉ TRAITÉE par D223** : la part est dite au rapport d'ouverture (« 2600 sur 3568, soit 73 % ») et un geste qui désigne une minorité existe (« Les 10 % les moins sûres », par le rang et non par un seuil ; 74 notes sur une piste de 738). **CLOSE par D224** : la vérité du corpus a répondu — la confiance sépare (27 → 38 → 49 → 60 % de notes justes de 0,00 à 0,65, aux trois tolérances), le seuil tombe là où la courbe croise la moitié et ne bouge pas ; la marque est large parce que les notes le sont. La mesure a ouvert B14~~ | `ROADMAP-daw.md` D223 → D224 | 53 à 91 % des notes marquées ; le geste neuf en marque 10 % par piste ; core 331 tests verts |
| ~~A40~~ | ~~**La fenêtre d'historique s'ouvrait vide**~~ — **CLOSE par D231** : `refreshHistoryList()` sort tout de suite quand la fenêtre est invisible (pour ne pas travailler à vide), et elle était appelée AVANT `setVisible(true)` ; l'historique ne se remplissait qu'à la modification suivante. Montrer d'abord, remplir ensuite. Trouvée en photographiant la fenêtre après une simple ouverture, ce que personne n'avait fait | `ROADMAP-daw.md` D231 | 0 pas affichés → **2** sur la même course |
| ~~A41~~ | ~~**Le premier point d'automation efface toutes les courbes du projet**~~ — **CLOSE par D234** : `AutomationComponent::lanes_` n'était jamais rempli depuis le projet (le moteur les recevait, le disque les gardait, le panneau jamais), et `commit()` publie `lanes_` par-dessus `currentAutomation_`, que `captureSessionIntoProject()` réécrit après avoir vidé `track.automation`. Mesuré sur `cdl` : une courbe de **606 points** sur `filter.1.cutoff`, un point posé, et le projet enregistré n'en portait plus qu'UN, sur un autre paramètre. `setLanes()` donne les courbes au panneau ; la liste marque les paramètres qui en portent (« Filter Cutoff ● 606 ») et l'onglet s'ouvre sur l'un d'eux | `ROADMAP-daw.md` D234 | 606 → **1** avant ; 606 → **607** après, témoin sans geste à 606 |
| ~~A6~~ | ~~La barre de transport prend deux rangées sur cet écran~~ — **CLOSE par D364, et sans toucher une ligne de code : le défaut était dans la MESURE.** Le « plafond de 1 280 px » est celui des BANCS (`VSM_TAILLE=1280x742`) ; l'écran de cette machine fait **3 200 × 2 000**. Mesuré par le relevé `VSM_TRANSPORT_ZONES` : 2 rangées et 100 px à 1 280 et 1 920 px, **1 rangée et 56 px dès 2 240 px**, donc sur l'écran réel — les 56 px que A6 donnait pour idéal. Le repli sur deux rangées reste le comportement juste d'une fenêtre étroite. Gardé par `tools/barre-transport.sh` (2 ratés au témoin), parce qu'une commande de plus repousserait le seuil au-delà de l'écran et ramènerait le défaut pour de bon | `ROADMAP-daw.md` D68 → D94 → **D364-D365** | 2 rangées → **1**, 100 px → **56** |
| ~~A42~~ | ~~Quinze façades sur 63 posent un bouton ou un curseur sous le plancher de 18 px au rack de la disposition par défaut~~ — **CLOSE par D293-D300 le jour même** : quinze façades recoupées sur deux rangées de sections (la façade grandissant en hauteur, ce que D292 permet), et le balayage complet rejoué à 16:00 rend **0 / 63**. *Constat d'origine* — 15 / 63 sous 18 px, 8 px au pire (l'enveloppe ADSR de `vsm.psg`, `vsm.vocal`, `vsm.westcoast` : quatre curseurs verticaux dans 3 colonnes sur 14) ; D64-D70 publiaient « 0 / 63 » sans nommer la largeur de leur mesure | `ROADMAP-daw.md` D292 → D300 ; garde `tools/balayer-facades.sh` (rend 1 tant qu'une façade passe dessous) | **15 / 63 → 0 / 63** sous 18 px |

### B. Ce que la mesure réclame

| # | Travail | Source | Pourquoi |
|---|---|---|---|
| B1 | **Réengendrer le corpus et réentraîner le classifieur de machine**, puis reprendre **A5.2** — **CORPUS FAIT, MODÈLE REFUSÉ (12/09, § A6.3-A6.4)** : 58 machines mélodiques (et non 59 — `vsm.fmdrums` est une boîte à rythmes, 4 800 rendus tous inaudibles), 273 061 exemples. Le modèle entraîné dessus fait **17,8 %** de top 1 et **15,6 % sur les 20 anciennes classes** contre 93,8 % : il n'est PAS adopté. Deux témoins écartent le corpus et le code (l'ancien corpus rend 93,8 % avec le code d'aujourd'hui ; le corpus neuf restreint aux 20 mêmes rend 94,6 %), et un plus proche voisin fait **77 %** sur les 58 — les descripteurs séparent, l'estimateur s'effondre. **H28 réfutée puis H29 TENUE le 12/09** (§ A6.5-A6.6) : tripler le budget ne change pas une décimale, mais changer de FAMILLE d'estimateur donne **83,6 %** de top 1 sur 58 machines, sans classe attracteur (2,4 % pour 1,8 % de part réelle), et fait tomber le **critère A1.1 pour la première fois sur le parc entier** (99,5 % de top 3 hors indistinguables). Le modèle à 58 machines est ADOPTÉ (`modeles/classifieur.joblib`, l'ancien gardé sous `classifieur-20-hgb.joblib`) ; **A5.2 rejouée et ATTEINTE POUR LE MODÈLE** (§ A6.8) : la désignation confiante sur un piano acoustique — `vsm.sh101` à 0,99, l'échec que A5.2 nommait — a DISPARU (0 confiante sur 20 extraits), témoin sur l'ancien modèle à l'appui ; réserve écrite, l'abstention passe de 90 % à 79 % parce qu'un parc de 58 machines couvre plus d'espace qu'un parc de 20. Reste A5.2 côté chaîne, la présélection apprise, désormais posable. *Historique* après un SECOND arrêt silencieux (48 machines sur 59, aucun manifeste, `pgrep` vide pendant que l'INDEX disait « en cours ») ; la reprise est gardée par une preuve et non par une lecture de commits — le binaire de rendu avait été recompilé entre-temps, et 993 exemples secs appariés par patch et par note sont identiques au bit près (§ A6.2) | `ROADMAP-apprentissage.md` A6, `ROADMAP-fusion.md` § 5 bis.7 | le modèle du 28/08 connaît **20 des 59 candidates mélodiques**. **Le chiffre de « 43 machines sans empreinte », écrit le 10/09 au matin, était faux** : il comptait comme manquantes `vsm.drums`, `vsm.tr808` et `vsm.tr909`, qui ont leur empreinte dans l'AUTRE modèle (`modeles/frappes.joblib`, phase A2). Le compte juste est **39 mélodiques à faire**, plus `vsm.sampler` qui relève du corpus de frappes |
| B2 | **Écouter** — A5.3 : l'A/B dans le DAW contre l'original, les notes douteuses une par une. **LA MATIÈRE EST PRÊTE ET REMISE (13/09)** : `reconstruction/travail/ecoutes/b4-AB-60s-a-90s-sans-ecretage.wav` et `b4-AB-150s-a-180s-sans-ecretage.wav` (les premiers écrêtaient, voir B10), deux extraits de 30 s de l'écoute A/B de « B4 Wuz Then » (gauche = original, droite = reconstruction, distance 0,1688). Le projet entier s'ouvre par `reconstruction/travail/b4wuzthen` | `ROADMAP-apprentissage.md` A5.3, `CDC-apprentissage.md` § 10 | « il reste exactement une chose : écouter » ; aucune mesure ne la remplace |
| ~~B3~~ | ~~Contre-épreuve de `htdemucs_6s` sur un troisième original~~ — **CLOS par la course 2 de l'épreuve Children** (12/09) : *Children* est le troisième original, et le seul des trois qui ait un vrai piano SANS guitare. `--modele htdemucs` (quatre stems) rend **0,2342** contre **0,1935** au défaut à six stems, soit **21,0 % plus loin** — la règle écrite d'avance (« plus loin d'au moins 2 % → le défaut est confirmé ») est franchie dix fois. À noter pour la réserve du § 4.2 : le gain ne vient PAS d'un stem `piano` qui tiendrait le motif (il ne le tient que de 28 à 57 s, tant qu'il est à nu) mais du partage général | `CDC-detection-multipiste.md` § 4.2, § 12.3 | −10,4 % sur un morceau à guitare et piano, **+3,9 %** sans eux, **−17,4 %** sur *Children* |
| B4 | **Écouter les deux wav de `--voix-tete-choeurs`** — les fuites de réverbération de la tête dans les chœurs ne se jugent qu'à l'oreille. **LA MATIÈRE EST PRÊTE ET REMISE (13/09)** : `reconstruction/travail/ecoutes/voix-tete-90s-a-120s.wav` et `voix-choeurs-90s-a-120s.wav`, trente secondes chacun, tirés de `children-c3-plafond` | `CDC-detection-multipiste.md` § 4.5 | distance mesurée neutre (+0,05 %) ; la qualité, non |
| ~~B5~~ **FAIT LE 19/09/2026 par D366** — le lot `s2` : dix morceaux de **186 à 269 s** (38,5 min), 82 parties, **13 à 16 sections** et de 2 à 11 parties qui entrent ou sortent par morceau, **22 891 notes mélodiques sous 120 ms** (contre **0** dans `s1-sec`, dont les 1 865 notes courtes étaient 1 865 frappes de batterie), **7 morceaux sur 10** à échantillons et **5 sur 10** chantés (4 `vsm.vocal`, 1 chœur échantillonné), désaccord **mesuré sur le rendu** au maximum de **1,63** demi-ton pour une borne de 2. Garde `tools/corpus-exigences.py` : **0/4 sur `s1-sec`, 4/4 sur `s2`**. Le défaut du générateur reste le corpus d'avant AU BIT PRÈS (cinq graines, quarante-quatre parties, témoin mesuré). Deux défauts du moteur trouvés au passage : les profils au-delà de 256 Mo refusés sans que le tirage en change, et un garde-fou de rendu qui bornait la durée (120 s) au lieu des échantillons — il laissait passer onze gigaoctets en lot et interdisait un morceau de quatre minutes. ~~Un lot de morceaux LONGS (3-5 min) avec des parties qui entrent et sortent~~ | **Un lot de morceaux LONGS (3-5 min) avec des parties qui entrent et sortent** — et, ajouté le 13/09 par D264-D269, **des notes mélodiques BRÈVES** (aucune aujourd'hui : les 1 865 notes sous 150 ms du corpus sont toutes des frappes) et **des paramètres de hauteur tirés sur une grille connue** (`*.detune`, `*.tune`, `oscillator.sub.level`), écrits dans `verite.json` comme facteurs de la mesure | `CDC-banc-synthetique.md` **§ 7 bis**, décisions au § 7 bis.4-5 | le banc mesurait une texture stable de 30 s ; deux mesures du 13/09 n'avaient pas pu aboutir faute de ces choses. Les deux gardes de corpus sont VERTES (`corpus-exigences` 4/4, `corpus-hauteurs` **CONTREDITES 0**). **B6 attend la campagne sur `s2`** — ~13 h (S1 a mis 1 h 43 sur des morceaux 7,7 fois plus courts) : `reconstruction/travail/banc-s2.sh` est prêt et refuse de partir tant que la garde n'est pas verte |
| B6 | **Le banc ne couvre ni les parties à échantillons ni la voix** — et, ajouté le 13/09, **son F1 porte les deux biais réfutés ce jour-là** : il compte la BATTERIE dans la vérité (que la chaîne compte ailleurs) et compare la hauteur ÉCRITE (quand 9,9 % des notes sonnent ailleurs). Il sous-estime donc la chaîne. **MESURÉ le 13/09 sur les dix morceaux de `r1f-13sep`** : F1 **0,4121 → 0,4839 (+7,2 pt)**, l'attendu tenu. **Mais l'écart n'est PAS stable — de +0,0 à +54,0 pt** : `morceau-0001-g1`, dont les deux parties mélodiques sont désaccordées de +4,75 et −8,25 demi-tons, n'a pas un F1 bas mais un F1 **qui ne veut rien dire** (0,027 au lieu de 0,567, le MEILLEUR des dix). La correction ne rebaselinera donc pas un chiffre : elle **change le classement des morceaux**, et toute comparaison entre morceaux de ce corpus est à revérifier. Le premier remède est **B5** (tirer les paramètres de hauteur sur une grille connue) | `CDC-banc-synthetique.md` § 7, `ROADMAP-fusion.md` § 9.6 | deux branches de la chaîne ne sont mesurées par rien, et la mesure qui existe est biaisée dans un sens connu |
| ~~B7~~ | ~~Terminer le lot forcé de R1, écrire le § 7.4 et signer la décision du § 7.5~~ **FAIT le 13/09/2026** | `CDC-separation-par-synthese.md` § 7.4-7.5 | lot `r1f-13sep` complet, **10 morceaux sur 10**, chaîne identique octet pour octet du premier au dernier. Les trois attendus de fond tenus : soustraction 10/10, montée du SDR du résidu vrai **−0,00113 dB**, basse au résidu **−0,0925 dB**. La boucle est inerte MÊME FORCÉE — ce n'était pas le garde-fou. § 7.5 signé ; l'hypothèse spectrale est désignée par la mesure |
| ~~B8~~ | ~~H27 — le témoin de coupure ne couvre pas les pistes sans machine~~ — **CLOS le 12/09** (§ 12.5) : il est établi pour toute piste JOUANTE (audio, ou machine avec notes), jamais pour un bus ; mesuré de bout en bout sur la course 3 — sans `Voix · tête` 0,1950, sans `Voix · chœurs` 0,1978, sans les deux 0,19353157517211156, soit la distance de la course 1 au dernier chiffre. Quatre tests le gardent. *Constat d'origine* : le sortir de la boucle des alternatives de `vsm_mix_verdict.py`. Attendu chiffré d'avance, vérifiable sans nouvelle course | `CDC-detection-multipiste.md` § 12.4 | deux pistes audio sont entrées au mélange de la course 3, l'ont dégradé de **2,39 %** (0,1935 → 0,1982), et **aucune ligne ne l'a dit** — la panne muette que ce témoin avait été écrit pour fermer |
| B9 | **H26 — un fourre-tout se reconnaît dans le TEMPS autant que dans la hauteur** — **premier pas RÉFUTÉ deux fois le 12/09** (§ 12.6-12.7) : la dispersion d'un profil de bandes, simple puis pondérée par l'énergie, ne sépare pas « plusieurs parties » de « résidu de séparation » ni de « batterie » — les stems de FUITE de *Children* (0,6577 et 0,6401) dominent le vrai fourre-tout (0,6176), et pondérée, `drums` passe devant. **La DEUXIÈME forme — compter les PALIERS plutôt que la dispersion — SURVIT à ses deux critères (§ 12.8-12.9)** : sur *Children*, `other` rend **4 timbres installés** et `bass` 3 (les deux stems à plusieurs parties), les deux stems de FUITE rendent **0** là où la dispersion les mettait en tête, et `piano` 1, son unique palier tombant dans la fenêtre où le piano est à nu. **Et la porte ne suffit pas (§ 12.10-12.11)** : `separer_en_voix` REJUGEAIT la densité — l'option était inerte, `other` restant à une voix pour 3 651 notes. Le garde-fou accepte désormais une justification MESURÉE, et le découpage obtenu coupe la HAUTEUR : quatre voix jouant chacune 81 à 99 % du morceau, quand les parties entrent et sortent. **H30 écrite, implémentée et RÉFUTÉE dans la foulée (§ 12.12)** : le découpeur de temps tient son critère (support réel 15/55/12/18 % contre 68-94 %) et rate son but — chaque voix couvre MIDI 29-96, l'ambitus entier, parce qu'une voix temporelle contient tout ce qui joue alors. Ce sont des SECTIONS, pas des parties, et leur donner une machine chacune ferait changer de machine un même instrument au pont. **H31 mesurée et RÉFUTÉE à son tour (§ 12.13)** — 3 voix d'ambitus 24, 17 et 43 demi-tons : combiner les deux dimensions ne fait pas mieux que la hauteur seule. **CONCLUSION DE B9, en quatre mesures** : la DÉTECTION marche (0 sur un résidu, 4 sur le vrai fourre-tout) et le DÉCOUPAGE ne peut pas marcher — sur ce disque les quatre parties d'`other` se recouvrent dans les deux dimensions à la fois. Le remède n'est pas dans le découpage des notes mais **en amont, dans la séparation de sources : le chantier C1**, que cet INDEX nomme déjà « le vrai plafond du projet ». *Ce qui reste à trouver* : une porte qui compare le profil spectral du stem entre ses sections, le tableau de partage étant déjà calculé | `CDC-detection-multipiste.md` § 12.4 | sur les trois courses de *Children*, le compte de pistes va de 7 à 11 et le nombre de parties tenues SEULES reste **3** — `other` porte quatre parties à 2,58 de polyphonie moyenne, et la porte en demande 3, si bien que le découpage n'est jamais ESSAYÉ |
| ~~B10~~ | **CLOS le 15/09** (aucune course ne tournait) : `ecrire_wav(…, ecoute=True)` descend les DEUX canaux du même facteur quand la crête dépasse −1 dBFS, le dit au journal et au rapport (`comparaison` : crêtes, `gainDb`, échantillons écrêtés évités) ; la sonde coupée de D282, entrée de mesure, n'est PAS descendue ; 4 tests (`test_comparaison_ecretage.py`). ~~`comparaison.wav` écrête quand la reconstruction dépasse 0 dBFS~~ — le fichier de l'écoute A/B est en 16 bits, et sur « B4 Wuz Then » la reconstruction dépasse de **2,7 dB** : **1 176 échantillons à fond d'échelle**, une distorsion que le musicien attribuerait à la reconstruction. `reconstruit.wav`, en flottant, garde la crête 1,4741 — c'est le graveur du fichier de comparaison, pas le rendu. Remède : descendre les DEUX canaux du même facteur (le rapport doit rester intact), le DIRE, ou écrire en flottant | `CDC-detection-multipiste.md` § 13 | 1 176 échantillons écrêtés ; extraits propres fabriqués à −4,37 dB en attendant |
| ~~B11~~ | **CORRIGÉ LE 18/09** (`CDC-detection-multipiste.md` § 14) : une passe FINALE rejoue le témoin de chaque piste jouante contre le projet LIVRÉ — même référence pour toutes —, un avis qui change entre le jugement et la livraison est DIT avec ses quatre chiffres, et les PAIRES de pistes signalées sont essayées (coût borné : 1 rendu + 1 par piste + 1 par paire signalée, mesuré à 5 rendus pour trois pistes dont deux signalées). Trois tests (211 → **214** Python) ; l'effet de bout en bout sur « B4 Wuz Then » attend la prochaine course. ~~**Le témoin de coupure est mesuré trop tôt, et jamais en combinaison** — sur « B4 Wuz Then », les deux témoins comparent 0,1679 et 0,1691 à **0,1889**, la distance AU VERDICT DU MÉLANGE ; la course a ensuite ramené le projet à **0,1688**. L'avis « le morceau est meilleur sans cette piste » est donc rendu contre un état dépassé, et il peut avoir cessé d'être vrai. Et couper DEUX pistes n'est pas la somme de deux coupes : les combinaisons ne sont jamais essayées. Remède au même endroit : rejouer le témoin sur le projet FINAL, et au moins essayer les paires parmi les pistes signalées~~ | `CDC-detection-multipiste.md` § 13 | 0,1889 au témoin contre 0,1688 au livré |
| ~~B12~~ | **CLOS — corrigé par D282 (`X: list = []`), constaté le 15/09 : `mypy analyse tools` rend « no issues found in 131 source files ».** ~~`mypy` signale une annotation manquante dans `vsm_classifier.py`~~ — `analyse/analyzer/vsm_classifier.py:76`, « Need type annotation for "X" » : les quatre listes de `X, machines, patchs, augmentations = [], [], [], []` n'ont pas de type, et `X` reçoit des tableaux. Un seul et unique signalement sur 47 fichiers ; `ruff` est propre. Arrivé avec le changement de famille du classifieur (H29). **Non corrigé aujourd'hui** : `CLAUDE.md` interdit de toucher `analyse/analyzer/*.py` pendant qu'une course tourne, et le lot forcé tourne | `analyse/analyzer/vsm_classifier.py:76` | mypy : **1 erreur sur 47 fichiers** ; ruff : « All checks passed » |
| ~~B13~~ | ~~Le menu Aide ne porte qu'une entrée~~ — **CLOS par D206** : la liste des raccourcis est de la DOCUMENTATION, pas une vue — elle n'affiche rien du projet, elle explique le logiciel —, et c'est sous *Aide* qu'un musicien la cherche, comme dans les trois logiciels de référence. **Déplacée et non dupliquée** : `VSM_MENU` prend le premier libellé exact tous menus confondus, et deux entrées du même nom rendraient ambigu tout banc qui l'emploie (la panne du 06/09). Mesuré : Aide porte deux entrées, Affichage n'en porte plus, le libellé reste unique dans toute la barre, et `VSM_MENU="Raccourcis clavier"` l'ouvre toujours | `ROADMAP-daw.md` D206 | menu Aide : **1 entrée → 2** ; occurrences du libellé : **1** |
| B15 | **CORRIGÉ LE 13/09 par D265 — lire d'abord ceci : l'instrument ne voyait pas la batterie.** `stems[].noteConfidence` ne porte AUCUNE percussion (la batterie a son propre compte, `drums.hits`), et les 1 865 frappes du corpus étaient comptées absentes en silence. Or elles sont les SEULES notes brèves du corpus : **1 865 sur 1 865 des notes sous 150 ms sont des frappes**, aucune partie mélodique n'en écrit une seule. Donc « 96,7 % des notes courtes ratées » (D260) et « 7,3 % de l'énergie » (D261) ne mesuraient que l'aveuglement de la mesure. Le chiffre juste, sur les dix morceaux de `r1f-13sep` : **rappel mélodique 54,8 %**, donc **45,2 % de manque** — B15 garde son rang de plus gros levier, pour une autre raison que celle qui était écrite. Et la chaîne écrit **1 426 frappes pour 1 865 vraies (76,5 %)**. Le remède n'est PAS dans la détection d'attaques. *Énoncé d'origine, conservé pour mémoire :* **41 % des notes jouées ne sont écrites NULLE PART** — mesuré sur 20 907 notes vraies de trois lots (D259, `tools/confiance-contre-verite.py`) : 37,8 % retrouvées au début exact, 12,3 % tenues depuis peu, 9,2 % à l'octave, **40,7 % absentes**. La chaîne écrit 13 287 notes là où le morceau en joue 20 907. **C'est le plus gros levier de la reconstruction** : corriger toutes les octaves (B14) laisserait le morceau amputé de 40 % de ses notes. **D260 dit LESQUELLES** : la chaîne rate **96,7 % des notes de moins de 150 ms** (4 661 sur 4 822), contre 38 % au-dessus de 500 ms — ce n'est pas le niveau (les absentes sont plus fortes) mais la DURÉE. Le remède est dans la détection d'attaques ; attendu écrit : les notes courtes retrouvées de 3,3 % à plus de 50 %, sans que la part inventée dépasse 3 %. **Et D261 dit pourquoi la chaîne les rate** : ces notes sont 22,5 % des notes mais **7,3 % de l'énergie** — la distance ne les compte presque pas, et la prudence de la chaîne est la réponse rationnelle à son propre score. Corriger la détection ne fera donc pas baisser la distance : il faut publier un COMPTE de notes retrouvées à côté d'elle. Demande `analyse/analyzer/` : après la course | `ROADMAP-daw.md` D259 | 64 % des notes écrites ; 40,7 % des vraies absentes |
| B14 | **RESSERRÉE LE 13/09 par D267-D268 — deux stems sur quatre passent déjà l'attendu.** Le corpus tire ses patchs au hasard et plusieurs machines exposent un désaccord d'oscillateur en demi-tons entiers (±24 sur `vsm.pcmhybrid`, ±12 sur `vsm.obx`) : **9,9 % des notes mélodiques du corpus SONNENT à côté de ce que leur liste annonce**, et le transcripteur a raison de les entendre là. Recomptée contre la hauteur ENTENDUE (`VSM_SONNANTE=1`) : `other` **72,6 → 83,4 %**, `guitar` 64,1 → 67,4 %, `piano` 78,3 → **79,2 %**, `bass` 61,6 → **61,6 %** (inchangée au dixième de point). L'attendu « plus de 75 % par stem » est donc **déjà tenu par `other` et `piano`** sans qu'une ligne de chaîne ait changé. Il reste **`guitar` et `bass`**, et pour la basse la cause est confirmée plutôt qu'écartée : sa part « octave » MONTE de 21,8 à 24,9 % quand on corrige le reste. *Énoncé d'origine, conservé pour mémoire :* **L'ambiguïté d'OCTAVE coûte un cinquième à un quart des notes de CHAQUE stem** (D254 : 20,3 % sur `other`, 25,5 % sur `guitar`, 27,5 % sur `piano`, 26,8 % sur `bass`, 13 287 notes de trois lots) — et la chaîne n'invente presque rien (0,4 à 1,2 %). Corriger les octaves porterait la bonne hauteur de 57-69 % à **77-97 %**. Attendu écrit : plus de 75 % sur chaque stem, sans que la part inventée monte. **Trois remèdes simples déjà éliminés par la mesure** (D257, D258) : le registre trompe deux fois sur trois, le « fantôme sous-octave » n'explique qu'une erreur sur six, et le contexte harmonique de la transcription plafonne à 46 % — le gros du remède est dans l'estimation de hauteur elle-même. **D256 le montre par l'autre bout** : 816 notes (6,1 %) sont écrites sous C2 ou au-dessus de C6, registres où la vérité du corpus ne joue JAMAIS — **aucune n'est juste**, et 60 à 66 % sont à une octave de la vraie. *Symptôme d'origine* : **la confiance s'INVERSE au-dessus de 0,65** — mesuré sur TROIS lots (`r1-sec-banc`, `r1-prod-banc`, `r1f-sec-banc` — 26 morceaux, 13 287 notes, `tools/confiance-contre-verite.py`) : la part de notes justes monte de 27 % à 60 % entre 0,00 et 0,65, puis REDESCEND dans [0,65 ; 0,80) — **trois fois la même forme** : 60 → 50, 59 → 48, 51 → 36 %, à 25, 50 et 100 ms de tolérance. **AFFINÉE par D232** : l'inversion vient ENTIÈREMENT de la BASSE (19 % de justes sur ses 294 notes les plus sûres, quand `other` et `guitar` montent), et les 81 % restants sont pour 41 % des re-articulations de notes TENUES (bonnes), 23 % des erreurs d'OCTAVE et 17 % seulement des notes inventées. La courbe recomptée en comptant les tenues : 55 → 65 → 77 → **89** → 80 %. Le grief a donc un nom : l'ambiguïté d'octave de la basse. **PRÉCISÉE par D252** : sur 1 755 notes de basse de trois lots, l'erreur d'octave n'est pas symétrique — **234 notes sont écrites une octave TROP BAS contre 32 trop haut**, soit 84 % des erreurs d'octave dans le même sens. Ce n'est pas du bruit, c'est un BIAIS, et le remonter porterait les hauteurs exactes de **27,1 % à 40,4 %** sur ce stem. Le biais est **propre à la basse** : `other` est symétrique (0,9×), `guitar` 1,8×, `piano` 1,4×, la basse **5,4×** — c'est le registre grave, pas l'estimateur en général. L'estimateur est sûr de lui là où il devrait l'être moins. Demande de toucher à `analyse/analyzer/` : après la course | `ROADMAP-daw.md` D224 | 27 → 38 → 49 → 60 → **50** % (50 ms) |

### C. Ce que S1 a réfuté, et qui appelle un chantier

La campagne S1 a mesuré la chaîne à vérité connue sur vingt morceaux et **elle
est plus faible que prévu à tous les étages**. Ces chiffres sont le vrai plafond
du projet ; toute autre optimisation est en aval d'eux.

| # | Travail | Chiffre |
|---|---|---|
| C1 | **La séparation de la basse est le premier plafond — D269 à D281 disent comment, et le premier remède qui améliore quelque chose est trouvé.** La séparation **empile du grave sous la fondamentale** (5,2 % de l'énergie sous 80 Hz dans la partie jouée, **49,8 % dans le stem**, et jusqu'à 99 % sur un morceau) ; une composante grave forte fait descendre le transcripteur d'une octave (**0,0 → 75,0 %** quand un sous-oscillateur monte, D278) ; et retirer ce grave au passe-haut semblait porter la bonne hauteur de 53,8 à 60,3 % — **mais la validation a ÉCHOUÉ** : réglée sur les morceaux 1-5 (+14 points), la coupure de 60 Hz fait **perdre 2,3 points** sur les morceaux 6-10, dont le témoin est déjà à 72,2 %. La bonne coupure dépend du REGISTRE de la basse, pas du modèle. **Le mécanisme reste établi, le remède à coupure FIXE est réfuté.** Mais une coupure **ADAPTÉE** au morceau (20ᵉ centile des hauteurs écrites, × 0,75 — aucun balayage) **tient l'attendu écrit** : +4,0 points sur la moitié de réglage et **+3,5 sur celle de validation**, octaves basses de 26 à **9** (−65 %), rapport bas/haut de 5,2 à **2,2×**. Premier remède qui survit à une validation — mais l'attendu ne portait pas son contrôle sur les justes cassées, et il en perd 9 % (151 → 137 sur B) : « validé » est une lecture d'après coup, le contrôle est écrit depuis (D281). La coupure ne retire aucune note vraie (0 sur 10 morceaux), et sur 4 morceaux de A sur 5 le centile tombe sous le registre vrai — la règle sous-coupe là où le défaut est le plus fort. Reste à savoir si l'échange vaut pour la RECONSTRUCTION, ce qui se mesure en bout de chaîne. *(Et il n'existe pas de lot témoin : les trois lots séparent les mêmes dix morceaux, leurs stems sont identiques au md5.)* **D269 dit aussi COMMENT elle échoue.** `htdemucs` rend une basse passée au filtre : la part de l'énergie au-dessus de 300 Hz tombe de **25,5 % dans la partie jouée à 1,7 % dans le stem** (médiane sur 9 morceaux, sous 3,3 % dans 8 sur 9). Ce sont exactement les partielles qui permettent de trancher une octave. Conséquence mesurée : sur le stem VRAI le transcripteur se trompe vers le HAUT (rapport bas/haut **0,3×**) ; sur le stem SÉPARÉ il se trompe vers le BAS (**6,9×**). **La moitié « basse » de B14 est donc C1 vue par un autre instrument** — corriger l'estimateur de hauteur ne rendra pas des harmoniques que le stem ne contient plus. **D282 (14/09) mesure la règle EN BOUT DE CHAÎNE** : attendu à quatre critères et contrôle (pas plus de 10 % de justes perdues) écrits AVANT la course (`ROADMAP-daw.md` D282), deux courses du banc sur les mêmes stems séparés, une seule variable. **VERDICT (course finie le 14/09 à 23:02, `tools/bilan-coupure-basse.py`) : 1 critère tenu sur 4** — le contrôle tient (−2,2 % de justes), mais la distance de la piste `bass` MONTE sur 8 morceaux sur 9 (médiane 0,3727 → 0,3812), la bonne hauteur ne gagne que +1,4 pt (il en fallait +3) et le mélange paie +0,71 % (plafond +0,5 %). **La coupure adaptée est REFUSÉE comme défaut** ; elle reste l'option `--coupure-basse-adaptee`, documentée par ce chiffre. Un stem meilleur à l'oreille du transcripteur n'est pas une reconstruction meilleure ; l'octave de la basse reste un défaut de SÉPARATION. | SDR **0,21 dB**, corrélation **0,26** — attendu ≥ 6 dB / ≥ 0,85 ; aigu de la basse **25,5 % → 1,7 %**. D273 : les trois modèles htdemucs filtrent pareil (2,2 / 3,0 / 2,8 %), le filtre est de FAMILLE ; mais `htdemucs_ft` rend **+1,6 dB de SDR** et **+45 % de notes justes** sur la basse, au prix de 4× le temps et de 4 stems seulement — d'où l'essai mené en D274 — et **RÉFUTÉ** : sur un morceau entier, la basse de `htdemucs_ft` fait MONTER la distance du stem de **0,3021 à 0,3407 (+12,8 %)** et la distance globale de **+2,12 %**, à parité de pistes inchangée (15) et pour 1,007× le temps. Une basse mieux séparée donne une reconstruction moins bonne, parce que **la distance d'un stem mesure son IMITABILITÉ par le parc, pas sa fidélité** — la machine retenue change (`vsm.scanned` → `vsm.stochastic`) et fait pire. Le lot complet n'est pas lancé |
| C2 | **La transcription est le second — mais une part de son erreur appartient à C1.** D269 a montré que, sur les stems VRAIS du corpus, le transcripteur se trompe vers le HAUT (rapport octave bas/haut **0,3×**) et non vers le bas : c'est la séparation qui retourne le biais à **6,9×** en retirant 94 % de l'aigu de la basse. Juger la transcription sur des stems séparés mesure donc les deux à la fois | F1 **0,369** sur les dix morceaux de `r1f-13sep` (0,367 auparavant) — attendu 0,50 à 0,70 ; le rappel (0,372) et la précision (0,377) s'y rejoignent, là où l'écart d'avant était de 8 points |
| C3 | **La séparation hallucine des sources**, et la chaîne les reconstruit consciencieusement | 19,8 % d'énergie hallucinée — la seule attente tenue, et c'est celle qui fait mal |

**LA BOUCLE RÉSIDUELLE A ÉTÉ ÉCRITE POUR ATTAQUER C1 ET C3, ET R1 L'A MESURÉE :
ELLE EST INERTE.** Zéro soustraction sur les vingt morceaux ; les dix-neuf
batteries candidates corrèlent à leur stem entre 0,001 et 0,049 quand le seuil
publié est 0,5 — réfuté d'un facteur quarante. Au niveau de l'échantillon, un
rendu ne ressemble pas assez à son stem pour en être soustrait, ni sur le banc
ni sur les deux disques. `--residuel` reste une option publiée, mesurée, et
**dite inerte dans son aide**.

L'hypothèse de repli est déjà écrite (§ 7.5 du CDC), pour ne pas être inventée
après : soustraire sur le **module du spectre** plutôt que sur l'échantillon,
la phase du mélange gardée. Elle ne s'écrira qu'une fois le lot forcé terminé.

### D. Documentation qui avait pris du retard — REFERMÉ le 10/09/2026

| # | Ce que le document annonçait | Ce qui est écrit maintenant |
|---|---|---|
| ~~D1~~ | `README` : « 53 machines », « 1 486 tests moteur » | **63 machines**, **1 946 tests** moteur et 168 Python ; « cinquante-deux façades » → soixante-trois |
| ~~D2~~ | `ROADMAP-daw` § 1.1 : `app/` à 7 303 lignes | le tableau reste **daté du 31/08** — c'est sa raison d'être, il justifie une phrase — avec un renvoi à l'état courant (32 873 lignes) |
| ~~D3~~ | `CDC-multipiste` § 4.3 : « le DAW ne montre pas ce que la chaîne sait » | l'écran du rapport porte partage, densités, bloc batterie, distance par stem, métrique et `gate` (D53) |
| ~~D4~~ | `ROADMAP-fusion` § 1 : « 33 machines, 13 effets » | 63 machines, 16 effets d'insert |
| ~~D5~~ | `ROADMAP-daw` D26 : la molette « en commençant par la flûte » | la flûte **n'est pas au registre** — hors build, résultat négatif assumé (`0edd7cc`) ; la phase porte sur six machines |
| ~~D6~~ | `MODE-EMPLOI` : « 53 machines · 13 effets · 1 081 paramètres · 1 486 tests » | **63 · 16 · 1 146 · 1 946**, relus au moteur |

Les cinq occurrences restantes de « 53 machines » et « 1 486 tests » dans les
cahiers des charges sont des **relevés datés** — un jalon, un moteur nommé dans
un A/B, un procès-verbal de recette. Elles restent telles quelles : un chiffre
historique qu'on rafraîchit cesse de dire ce qu'il disait.

### E. Dette d'outillage — REFERMÉ le 10/09/2026

| # | Ce que c'était | Ce qui est fait |
|---|---|---|
| ~~E1~~ | 54 signalements `ruff` dans `analyse/` et `tools/` | **zéro**. 18 corrections automatiques relues avant application ; les 13 `zip()` tranchés **un par un** (11 en `strict=True`, où les deux suites sont construites ensemble et le contrôle ne peut pas se déclencher ; 2 en `strict=False` avec leur raison écrite) ; le reste à la main |
| ~~E2~~ | rien ne lançait `ruff` ni `mypy` | `./verifier.sh` passe les cinq suites du moteur, la suite Python, `ruff` et `mypy` en une commande, et **nomme ce qu'il a sauté** |

**Une correction de lint a cassé le code, et le lint l'a rattrapée.** En
renommant `l` en `ligne` dans `epreuve_parite.py`, le corps de la boucle est
resté sur l'ancien nom : `ruff` a rendu quatre `F821 Undefined name 'l'` avant
qu'aucun test ne tourne. C'est l'argument pour le garde-fou, écrit par le
garde-fou lui-même.

---

## 5. Ce qui n'est pas au programme, et pourquoi

Rassemblé ici pour qu'on ne le repropose pas : chaque refus est mesuré ou
argumenté dans son document.

- **Une API réseau** — le tube JSON suffit (`ROADMAP-interop.md` § 0).
- **Des machines de caractère ajoutées pour la couverture** — elles élargissent
  le catalogue, pas la couverture ; à faire pour le plaisir de jouer, jamais en
  s'en réclamant pour la reconstruction (`ROADMAP-fusion.md` § 6). *L'élargissement
  du vivier reste une demande permanente, à ce titre-là et pas à l'autre.*
- **Un modèle appris de bout en bout (audio → paramètres)** — mené jusqu'au
  bout, mesuré, refusé (`ROADMAP-fusion.md` § 7).
- **Reconstruire la voix par synthèse** (`ROADMAP-fusion.md` § 6).
